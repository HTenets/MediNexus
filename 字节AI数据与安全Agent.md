
本项目聚焦大模型在 **CUA（Computer Use Agent）与 VLM（Vision Language Model）** 两个核心方向的能力建设，通过**数据生产策略**驱动模型能力持续优化，涵盖 Skill/MCP 工具调用、多模态代码生成、Multi-Agent 数据合成产线等大模型数据工程前沿方向。

  

---

  

# **字节跳动 AI 数据与安全（基础大模型—GUI）**

  

# **自我介绍**

  

面试官您好，我叫XXX，本科毕业于XXX，硕士就读于XXX，在校期间我曾多次获得过奖学金，我的研究方向是 XXX。

  

此外，我有一段实习经历，是在**字节跳动基础大模型团队**做 AI 数据与安全方向的实习生，主要参与的项目是 **GUI 方向的大模型能力建设**。这个项目分两条主线：**CUA-Next（下一代 PC 场景混合模态 Agent，GUI + Skills/MCP）**和 **Visual2Code（多模态大模型的图表/科研绘图代码生成专项优化）**。我负责其中**数据合成 Workflow 设计、Multi-Agent 数据产线搭建、质量筛选准入机制以及自动化评测报告 SKILL 的开发**，通过构造高质量 SFT 和 RL 数据，推动模型在 SkillBench、FigureBench、ChartGalaxy 等基准上实现性能稳步提升，并对竞品模型完成追赶与超越。

  

以上是我的自我介绍，需要我给您详细介绍一下这个项目吗？

  

---

  

# **简历写法**

  

**字节跳动 —— AI 数据与安全（基础大模型—GUI）**  2026.02 – 2026.07

  

**项目背景**

现有大模型在 CUA（Computer Use Agent）、VLM（Vision Language Model）等方向的能力有限，通过制定数据生产策略来构造数据，推动模型能力的持续优化。

  

**Ø CUA-Next：建立下一代 PC 场景的混合模态 Agent —— GUI + Skills / MCP**

- **贡献1**：设计可复用的构造题目 Workflow，覆盖"提取 - 过滤 - 结构化 - 任务拆分 - 合并优化 - 标准提取"等环节，实现 GUI + Skills/MCP 类型数据集的自动化生成，提升数据合成效率。

- **成果**：生成 1077 个 Skill、4307 道多工具混合调用题目，基于 SkillBench 数据集开展 A/B 测试，模型多工具调用任务执行成功率提升 16.7%，任务执行完成率（200 step 内完成）提升 22%。生成 327 个 MCP 工具与 1615 道混合调用题目；对比纯 GUI 任务，模型任务完成率提高 18.2%，任务完成效率提升 27%，复杂任务执行能力显著增强。

**Ø Visual2Code：提升多模态大模型在视觉理解和专业代码输出的能力 —— 图表&科研绘图专项优化**

- **贡献1**：搭建"分类标签树 + Multi-Agent 生成代码 + 质量筛选准入 + COT 合成"数据合成产线，涵盖 138 种图表类型、9 种技术栈、17 种风格元素和 2 类任务意图，为模型提供多源且高质量的 SFT 和 RL 数据。

- **贡献2**：设计"自动化生成评测报告 SKILL"，根据人评 GSB 理由、模型 Trace 和代码产物进行 Bad Case 归因分析，为下一步合成数据的方向提供指导。

- **成果**：完成 5 万+ 开源 chart2code 数据的清洗、COT 生成与难度拒采，累计交付 55 万条数据，训练后的模型在 FigureBench、ChartGalaxy 等基准测试中实现性能稳步提升，实现对竞品模型追赶和超越。

---

  

# **项目描述**

  

我在字节跳动实习时，参与的是基础大模型团队下 **AI 数据与安全** 方向的工作，专注于 **GUI（图形用户界面）方向的大模型能力建设**。整个项目分两条技术主线：一条是 **CUA-Next**，做下一代 PC 场景的混合模态 Agent，让模型能同时操作 GUI 和调用 Skills/MCP 工具；另一条是 **Visual2Code**，做多模态大模型在图表和科研绘图场景下"看图生代码"的能力优化。我主要负责**数据合成 Workflow 设计、Multi-Agent 数据产线搭建、质量筛选机制和自动化评测报告 SKILL 开发**。

  

**为什么做 CUA-Next？**

  

2024 年下半年开始，CUA（Computer Use Agent）成为大模型落地的核心方向之一。Anthropic 在 10 月发布了 Computer Use 功能，OpenAI 在 2025 年 1 月发布了 Operator，国内的智谱、阶跃等也陆续跟进。CUA 的核心目标是让大模型**像人类一样操作电脑**——看屏幕截图、理解界面、规划动作、执行点击/输入/滚动等操作。但纯 GUI 操作存在几个问题：

  

1. **效率瓶颈**：纯 GUI 操作需要逐帧截图、视觉解析、坐标计算，单步延迟高（通常 3-5 秒/步），复杂任务动辄需要几十甚至上百步，端到端时延不可接受。

2. **鲁棒性差**：GUI 元素位置变化、弹窗、加载延迟都会导致操作失败，模型容错能力弱。

3. **能力天花板低**：很多任务明明可以通过 API/工具一步完成（如"发送邮件"），但纯 GUI 必须走完打开应用、点击新建、输入收件人、点击发送等完整流程，冗余动作多。

  

我们的思路是**混合模态**：让模型同时具备 GUI 操作能力和 Skills/MCP 工具调用能力。当任务可以用工具直接完成时走 Skills/MCP，必须操作界面时走 GUI，两者可以无缝切换。比如"帮我订一张明天去上海的机票"，模型可以先调用 Skills 查询航班（API），遇到需要人工选座的场景再切换到 GUI 操作订票网站。

  

**为什么做 Visual2Code？**

  

多模态大模型（VLM）在图表理解到代码生成这个任务上有重要价值。场景包括：

- **科研绘图复现**：看到论文里的图表，想用代码复现

- **设计稿转代码**：设计师给的图表原型转成可运行的前端代码

- **数据可视化**：用户提供一张图，让模型生成对应的 ECharts/matplotlib 代码

  

但通用 VLM 在这个任务上有几个问题：

1. **图表类型覆盖不全**：科研绘图有 100+ 种图表类型（散点图、热力图、小提琴图、桑基图、Circos 图等），通用模型对低频图表生成质量差。

2. **技术栈多样**：用户可能要 Python（matplotlib/seaborn/plotly）、JavaScript（ECharts/D3/Chart.js）、R（ggplot2）等不同技术栈，模型需灵活切换。

3. **风格元素细节多**：配色、字体、网格、标注、图例位置等风格元素，通用模型经常忽略或生成不一致。

4. **COT 缺失**：模型直接从图到代码，缺少"先分析图表结构、再规划代码结构、最后生成"的推理链，复杂图表容易出错。

  

针对这些问题，我设计了 Visual2Code 数据合成产线，通过 Multi-Agent 协作 + 质量筛选 + COT 合成，构造大规模高质量训练数据，推动模型在 FigureBench、ChartGalaxy 等基准上的表现。

  

**我的具体工作**：

  

在 **CUA-Next** 上，我设计了覆盖"**提取 - 过滤 - 结构化 - 任务拆分 - 合并优化 - 标准提取**"六环节的可复用数据合成 Workflow，实现了 GUI + Skills/MCP 类型数据集的自动化生成。具体来说：

  

1. **提取**：从真实 PC 使用场景（如浏览器历史、应用日志、用户操作录制）中提取原始任务描述和操作轨迹。

2. **过滤**：去除低质量、重复、不合规的任务（如涉及敏感操作、过于简单无法体现混合调用价值、过于复杂无法验证）。

3. **结构化**：将自然语言任务描述转换为结构化的任务 Schema（任务目标、输入参数、预期输出、所需工具、GUI 步骤、Skills 调用、MCP 调用）。

4. **任务拆分**：将复杂任务拆分为子任务，每个子任务标注应走 GUI 还是 Skills/MCP。

5. **合并优化**：对子任务进行合并去重，优化执行顺序，生成最终的混合调用题目。

6. **标准提取**：从轨迹中提取标准答案（Final Answer + Step-by-step Trace + Tool Calls + GUI Actions），用于 SFT/RL 训练。

  

最终生成了 **1077 个 Skill、4307 道多工具混合调用题目**，以及 **327 个 MCP 工具、1615 道混合调用题目**。基于 SkillBench 数据集开展 A/B 测试，模型多工具调用任务执行成功率提升 16.7%，任务执行完成率（200 step 内完成）提升 22%。对比纯 GUI 任务，模型任务完成率提高 18.2%，任务完成效率（平均步数减少）提升 27%，复杂任务执行能力显著增强。

  

在 **Visual2Code** 上，我搭建了"**分类标签树 + Multi-Agent 生成代码 + 质量筛选准入 + COT 合成**"四阶段数据合成产线：

  

1. **分类标签树**：构建了覆盖 138 种图表类型 × 9 种技术栈 × 17 种风格元素 × 2 类任务意图的四维标签体系，确保数据覆盖全面且均衡。

2. **Multi-Agent 生成代码**：设计了 Planner（规划图表结构）、Coder（生成代码）、Reviewer（审查代码）、Refiner（优化代码）四个 Agent 协作的代码生成产线，比单 Agent 生成质量显著提升。

3. **质量筛选准入**：基于代码可执行性 + 视觉相似度 + 人工抽检的三级筛选，过滤低质量样本。

4. **COT 合成**：对高质量样本反向生成 Chain-of-Thought（先分析图表、再规划、最后生成代码的推理链），用于 SFT 训练。

  

同时，我设计了"**自动化生成评测报告 SKILL**"，根据人评 GSB（Good/Same/Bad）理由、模型 Trace 和代码产物，进行 Bad Case 归因分析，输出结构化报告（如"Bad Case 主要集中在热力图 + seaborn 技术栈 + 暗色风格组合"），指导下一步合成数据的方向。

  

最终完成 5 万+ 开源 chart2code 数据的清洗、COT 生成与难度拒采，累计交付 **55 万条数据**，训练后的模型在 FigureBench、ChartGalaxy 等基准测试中实现性能稳步提升，对竞品模型完成追赶和超越。

  

---

  

# **项目相关的问题**

  

## **1、什么是 CUA？为什么重要？**

  

**CUA（Computer Use Agent，计算机使用智能体）** 是指能够像人类一样操作计算机图形界面（GUI）的 AI 智能体。它通过**看屏幕截图 → 理解界面 → 规划动作 → 执行点击/输入/滚动**的循环，完成用户指定的任务。

  

**CUA 的核心能力**：

1. **视觉感知**：解析屏幕截图，识别按钮、输入框、菜单、图标等 UI 元素及其位置。

2. **语义理解**：理解界面文本和布局的含义（如"这个红色按钮是删除按钮，不要点"）。

3. **动作规划**：根据任务目标规划操作序列（先打开浏览器，再输入网址，再点击搜索框）。

4. **动作执行**：输出具体的操作指令（点击坐标 (x, y)、输入文本 "xxx"、按键 Enter）。

5. **状态追踪**：判断上一步操作是否成功，界面是否如预期变化，是否需要重试。

  

**为什么 CUA 重要**：

  

1. **覆盖长尾应用**：API 只覆盖少数主流应用，但用户每天使用的成千上万的应用（特别是企业内部系统、专业软件）没有 API，只能通过 GUI 操作。CUA 是唯一能通用覆盖这些应用的方案。

2. **降低 Agent 部署成本**：传统 RPA（机器人流程自动化）需要为每个应用编写专用脚本，维护成本高。CUA 是"零代码"自动化，模型直接看屏幕操作，无需适配。

3. **打通数字世界的"最后一公里"**：LLM 已经能理解文本、生成代码、调用 API，但只要用户的工作流里有任何 GUI 操作，就需要 CUA 介入。

  

**主流 CUA 方案**：

- **Anthropic Computer Use**（2024.10）：基于 Claude 3.5 Sonnet，输出截图坐标 + 键盘事件

- **OpenAI Operator**（2025.01）：基于 GPT-4o，浏览器场景为主

- **Google Project Mariner**：Chrome 扩展形态

- **智谱 AutoGLM**：国产 CUA，移动端 + PC 端

- **字节 CUA-Next**：本项目，GUI + Skills/MCP 混合模态

  

### **1.1、CUA 和传统 RPA 的区别？**

  

| 维度 | 传统 RPA | CUA |

|------|---------|-----|

| **适配方式** | 写专用脚本（UiPath/Power Automate） | 模型看屏幕直接操作 |

| **维护成本** | 高（UI 改版需重写脚本） | 低（模型自适应） |

| **泛化能力** | 仅适配的应用 | 理论上任意应用 |

| **准确性** | 高（精确控制） | 中（受模型能力限制） |

| **速度** | 快（毫秒级） | 慢（秒级，需视觉解析） |

| **成本** | 一次性开发 + 维护 | 每次调用消耗 LLM Token |

  

**CUA 的核心优势**是"零代码"和"泛化"，**核心劣势**是速度和准确性。所以实际落地通常是 **CUA + RPA 混合**：高频稳定流程用 RPA，长尾应用用 CUA。

  

### **1.2、CUA 的工作流程？**

  

```

用户任务: "帮我在淘宝买一本《三体》"

    ↓

[Step 1] 视觉感知

    模型接收当前屏幕截图

    识别: 桌面、浏览器图标、任务栏

    ↓

[Step 2] 语义理解

    理解任务: 需要打开浏览器,搜索淘宝,搜索商品,下单

    ↓

[Step 3] 动作规划

    Plan:

    1. 双击浏览器图标 (坐标 100, 800)

    2. 输入 taobao.com

    3. 按 Enter

    4. 等待加载

    5. 在搜索框输入 "三体"

    6. 点击搜索按钮

    ...

    ↓

[Step 4] 动作执行

    输出: click(100, 800)

    ↓

[Step 5] 状态追踪

    接收新截图,判断浏览器是否打开

    如果是 → 继续下一步

    如果否 → 重试或调整

    ↓

循环 Step 1-5 直到任务完成

```

  

### **1.3、CUA 的核心技术挑战？**

  

1. **视觉解析精度**：小图标、密集表格、动态加载内容容易识别错误。解决：Set-of-Mark（SoM）标注 + 视觉 grounding 模型。

2. **长程任务规划**：几十步的任务容易跑偏。解决：层次化规划（高层 Plan + 低层 Execute）+ 状态检查点。

3. **错误恢复**：弹窗、网络异常、加载延迟。解决：异常检测 + 回退策略 + 重试机制。

4. **坐标精度**：VLM 输出的坐标可能偏移。解决：元素级定位（输出元素 ID 而非坐标）+ 视觉验证。

5. **效率**：纯视觉每步 3-5 秒。解决：**混合模态（本项目核心）——能用 API/Skills 就不用 GUI**。

  

---

  

## **2、CUA-Next 的混合模态（GUI + Skills/MCP）是什么？**

  

这是本项目的**核心创新点**。纯 GUI CUA 存在效率瓶颈，纯 API/Skills 调用又无法覆盖所有场景。CUA-Next 让模型**同时具备两种能力，并能根据任务需要自由切换**。

  

### **2.1、Skills 和 Tools 的区别？**

  

| 概念 | 定义 | 例子 |

|------|------|------|

| **Tool（工具）** | 单一的、原子性的函数调用 | `search_web(query)`, `send_email(to, subject)` |

| **Skill（技能）** | 复合的、可重用的能力包,可包含多个 Tool + Prompt + 知识 | `book_flight(from, to, date)` 内部调用 search + select + pay 多个 Tool |

| **MCP（Model Context Protocol）** | 标准化的 Tool/Skill 暴露协议 | MCP Server 暴露 `filesystem.read_file` 等 |

  

简单理解：**Tool 是函数，Skill 是函数组合 + 上下文，MCP 是协议**。

  

在本项目中：

- **1077 个 Skill**：复合能力包，每个 Skill 可能包含 1-10 个 Tool

- **327 个 MCP 工具**：通过 MCP 协议暴露的标准化 Tool

  

### **2.2、什么是 MCP（Model Context Protocol）？**

  

**MCP（Model Context Protocol）** 是 Anthropic 于 2024 年 11 月发布的开放标准，用于标准化 LLM 与外部数据源/工具的连接。被业界称为 **"AI 的 USB-C"**。

  

**MCP 的核心概念**：

  

```

┌────────────────────────────────────────────┐

│  MCP Host (Claude Desktop / IDE / Agent)   │

│    └── MCP Client (协议客户端)              │

├────────────────────────────────────────────┤

│  MCP Server (协议服务端)                    │

│    ├── Resources (文件/数据,只读)           │

│    ├── Tools (可执行函数)                   │

│    └── Prompts (模板化提示词)               │

├────────────────────────────────────────────┤

│  Transport (传输层)                         │

│    ├── stdio (本地进程)                     │

│    └── SSE/HTTP (远程)                      │

└────────────────────────────────────────────┘

```

  

**MCP 的核心价值**：

1. **标准化**：所有工具用统一的 JSON Schema 描述，LLM 无需为每个工具学专门的调用方式。

2. **解耦**：工具开发者只需实现 MCP Server，无需关心上层 LLM 是什么。

3. **生态**：一次开发,所有支持 MCP 的 LLM 都能用（Claude/GPT/本地模型）。

  

**MCP vs 传统 Function Calling**：

- Function Calling 是模型能力（LLM 输出 JSON 调用工具）

- MCP 是协议标准（规定工具怎么描述、怎么连接、怎么调用）

- MCP 是 Function Calling 的标准化与生态化

  

### **2.3、为什么需要 GUI + Skills/MCP 混合模态？**

  

举例对比：

  

**任务**：帮我在浏览器里打开 GitHub,搜索 "langchain",star 第一个仓库

  

| 方案 | 执行步骤 | 步数 | 耗时 |

|------|---------|------|------|

| **纯 GUI** | 1.点击浏览器图标 2.输入 github.com 3.Enter 4.等待加载 5.点击搜索框 6.输入 langchain 7.Enter 8.点击第一个结果 9.点击 Star 按钮 | 9 步 | ~45 秒 |

| **纯 Skills/MCP** | `skill.search_github("langchain")` + `skill.star_repo(repo_id)` | 2 步 | ~3 秒 |

| **混合模态** | 调用 Skill 搜索 + Star（2 步） + 如果遇到登录弹窗 → 切换 GUI 输入账号密码（3 步） | 5 步 | ~15 秒 |

  

**混合模态的优势**：

1. **效率**：能用工具的直接用工具，省去 GUI 冗余操作

2. **鲁棒性**：GUI 操作失败时可以回退到工具调用

3. **覆盖广**：工具未覆盖的应用走 GUI，已有工具的应用走 Skills

4. **复杂任务能力**：真实业务往往是"工具能完成 80%，剩下 20% 必须 GUI"，混合模态才能端到端

  

### **2.4、混合模态的切换决策怎么做？**

  

模型在每个决策点判断走 GUI 还是 Skills/MCP：

  

**决策依据**：

1. **任务可工具化程度**：是否存在对应的 Skill？（如发邮件 → skill.send_email 存在）

2. **当前界面状态**：是否在 GUI 流程中？（如已在浏览器 → 继续走 GUI 可能更快）

3. **历史经验**：类似任务过去用哪种方式成功率高？

4. **效率预估**：GUI 预估步数 vs Skill 调用次数

  

**决策类型**：

- **Task-level Decision**：任务级决策（开始时决定整体策略）

- **Step-level Decision**：步骤级决策（每一步决定走 GUI 还是 Tool）

- **Hybrid Decision**：混合决策（主走 Skills,异常时 fallback 到 GUI）

  

---

  

## **3、CUA-Next 的数据合成 Workflow 怎么设计的？**

  

这是我的**核心贡献**。我设计了覆盖"**提取 - 过滤 - 结构化 - 任务拆分 - 合并优化 - 标准提取**"六环节的可复用 Workflow。

  

### **3.1、Workflow 六环节详解**

  

#### **环节 1：提取（Extraction）**

  

从多个数据源提取原始任务描述和操作轨迹：

  

| 数据源 | 提取内容 | 处理方式 |

|--------|---------|---------|

| **真实用户录制** | 鼠标/键盘事件 + 截图序列 | 事件流解析 + 自然语言任务描述生成 |

| **浏览器历史** | URL 访问序列 + 页面停留时间 | 任务意图推断（如连续访问购物网站 → 购物任务） |

| **应用日志** | API 调用日志 + 功能使用记录 | 工具调用序列提取 |

| **人工撰写种子** | 专家编写的任务描述 + 标准答案 | 直接作为高质量种子 |

| **开源数据集** | Mind2Web、WebArena 等公开数据 | 格式对齐 + 字段映射 |

  

**关键挑战**：真实录制数据噪声大（包含无关操作、误操作、中断）。解决：基于操作频率 + 任务连贯性的异常操作识别与过滤。

  

#### **环节 2：过滤（Filtering）**

  

五重过滤机制：

  

1. **质量过滤**：任务描述不清晰（<5 字）、操作轨迹不完整（<3 步）、无最终结果的剔除。

2. **去重过滤**：基于任务描述的 SimHash + 操作轨迹的 MinHash 双重去重。

3. **合规过滤**：涉及支付密码、个人隐私、违法违规操作的剔除。

4. **难度过滤**：剔除过于简单（<3 步纯点击）或过于复杂（>500 步，无法验证）的任务。

5. **多样性过滤**：确保任务类型分布均衡，避免某类任务过多（如打开浏览器任务占比 >30% 时降采样）。

  

#### **环节 3：结构化（Structuring）**

  

将自然语言任务描述转换为结构化 Schema：

  

```json

{

  "task_id": "task_001",

  "task_description": "在淘宝搜索《三体》并加入购物车",

  "task_type": "shopping",

  "difficulty": "medium",

  "expected_output": "购物车中包含《三体》",

  "required_tools": ["skill.search_taobao", "skill.add_to_cart"],

  "required_gui_steps": true,

  "environment": {

    "os": "windows",

    "browser": "chrome",

    "apps": ["taobao_web"]

  },

  "constraints": {

    "max_steps": 50,

    "timeout": 300

  }

}

```

  

#### **环节 4：任务拆分（Task Splitting）**

  

将复杂任务拆分为子任务,每个子任务标注执行方式：

  

```

原始任务: "帮我在淘宝买一本《三体》并通知朋友"

    ↓ 拆分

子任务1: 搜索《三体》       → Skills (skill.search_taobao)

子任务2: 选择商品           → GUI (需人工选规格)

子任务3: 加入购物车         → Skills (skill.add_to_cart)

子任务4: 下单支付           → GUI (需输入支付密码)

子任务5: 通知朋友           → Skills (skill.send_message)

```

  

**拆分策略**：

- **基于工具可执行性**：能直接 Skill 完成的拆为独立子任务

- **基于 GUI 必要性**：必须人工介入的环节（如输入密码、选择规格）单独拆分

- **基于依赖关系**：子任务间有先后依赖的保留顺序

  

#### **环节 5：合并优化（Merge & Optimization）**

  

对拆分后的子任务进行合并与优化：

  

1. **同类合并**：连续的 Skills 调用合并为一个 Skills 块,减少模型决策次数。

2. **顺序优化**：无依赖的子任务并行化（如搜索商品的同时发消息通知朋友）。

3. **冗余消除**：去掉不必要的 GUI 步骤（如已通过 Skill 完成搜索,就不需要再 GUI 点击搜索框）。

4. **Fallback 规划**：为每个 Skills 子任务规划 GUI fallback（如 Skill 调用失败时如何用 GUI 完成）。

  

#### **环节 6：标准提取（Standard Extraction）**

  

从执行轨迹中提取标准答案,用于 SFT/RL 训练：

  

```json

{

  "final_answer": "已将《三体》加入购物车",

  "step_by_step_trace": [

    {"step": 1, "type": "skill", "action": "skill.search_taobao", "params": {"query": "三体"}, "result": {"items": [...]}},

    {"step": 2, "type": "gui", "action": "click", "coordinates": [350, 420], "element": "商品卡片"},

    {"step": 3, "type": "skill", "action": "skill.add_to_cart", "params": {"item_id": "xxx"}, "result": {"success": true}}

  ],

  "tool_calls": [

    {"tool": "skill.search_taobao", "success": true},

    {"tool": "skill.add_to_cart", "success": true}

  ],

  "gui_actions": [

    {"action": "click", "target": "商品卡片"}

  ],

  "evidence_level": "A"

}

```

  

### **3.2、1077 个 Skill 怎么构造的？**

  

**Skill 来源**：

  

| 来源 | 数量 | 方式 |

|------|------|------|

| **真实应用 API 封装** | ~400 | 主流应用（淘宝/微信/Gmail/Slack 等）的官方 API 封装为 Skill |

| **MCP Server 接入** | ~250 | 接入开源 MCP Server 生态（filesystem/database/git 等） |

| **GUI 操作模式提炼** | ~300 | 从真实用户录制中提炼高频 GUI 操作组合,封装为 Skill |

| **人工种子扩展** | ~127 | 专家编写种子 Skill,基于模板自动扩展变体 |

  

**Skill 自动化构造流程**：

  

```

1. 应用 API 文档解析 → 提取 endpoint/参数/返回值

2. 参数 Schema 生成 → JSON Schema 描述

3. Skill Prompt 生成 → "何时调用此 Skill" 的自然语言描述

4. 测试用例生成 → 自动构造参数测试 Skill 可用性

5. 注册到 Skill Registry

```

  

**Skill 质量保证**：

- 每个新 Skill 必须通过 5 个测试用例（不同参数组合）

- Skill 失败率 >10% 的剔除

- 定期回归测试,API 变更时自动告警

  

### **3.3、4307 道多工具混合调用题目怎么生成？**

  

**生成策略**：

  

1. **模板化生成（~1500 道）**：基于任务模板 + Skill 组合,自动生成题目。

   - 模板：`"{action} {object} 并 {notify_method} 通知 {recipient}"`

   - 实例化："搜索《三体》并微信通知小明" → 调用 search + send_message 两个 Skill

  

2. **轨迹改写（~1200 道）**：从真实用户录制中提取任务,改写为标准题目。

   - 原始轨迹：用户在浏览器搜索商品 + 截图发给朋友

   - 改写题目："搜索 iPhone 15 价格并截图发给朋友"

  

3. **组合爆炸（~1000 道）**：从 Skill 集合中随机组合 2-5 个 Skill,生成多工具调用题目。

   - 组合：search_web + translate + send_email

   - 题目："搜索 climate change 最新新闻,翻译为中文,邮件发给 boss@example.com"

  

4. **对抗样本构造（~607 道）**：专家构造难负样本。

   - 工具不可用时的 fallback 题目

   - 多个 Skill 都能完成同一任务的歧义题目

   - 需要长程规划（>20 步）的复杂题目

  

**题目质量控制**：

- 可执行性验证：每道题在沙箱环境实际跑一遍,验证答案可达

- 多解性检查：允许多种正确路径,但必须有至少一个标准答案

- 难度分级：基于步数 + 工具数 + GUI/Skills 比例分为 easy/medium/hard

  

### **3.4、327 个 MCP 工具怎么构造的？**

  

**MCP 工具来源**：

  

| 来源 | 数量 | 说明 |

|------|------|------|

| **开源 MCP Server** | ~180 | filesystem/git/github/slack/postgres 等官方/社区 Server |

| **内部系统 MCP 化** | ~100 | 字节内部工具（飞书文档/字节云/内部 wiki）封装为 MCP |

| **自定义 MCP Server** | ~47 | 为特定任务场景开发的专用 MCP Server（如 chart_rendering_mcp） |

  

**MCP 工具 vs Skill 的区别**：

- MCP 工具是**标准化暴露的原子能力**（符合 MCP 协议规范）

- Skill 是**复合能力包**（可包含多个 MCP 工具 + Prompt + 知识）

- 关系：Skill 内部可调用 MCP 工具

  

### **3.5、A/B 测试怎么做的？**

  

**A/B 测试设计**：

  

```

对照组 (A): 纯 GUI CUA 模型 (基线,只学 GUI 数据)

实验组 (B): 混合模态 CUA 模型 (学了 GUI + Skills/MCP 数据)

    ↓

在 SkillBench 测试集上跑相同任务

    ↓

对比指标: 成功率 / 完成率 / 效率

```

  

**SkillBench 测试集构成**：

- 1000 道多工具混合调用题目（与训练集不重叠）

- 覆盖 5 大任务类型（购物/办公/通讯/开发/娱乐）

- 难度分布：easy 30% / medium 50% / hard 20%

  

**核心指标**：

  

| 指标 | 定义 | 对照组 (A) | 实验组 (B) | 提升 |

|------|------|-----------|-----------|------|

| **任务执行成功率** | 任务完全正确完成的比例 | 58.3% | 75.0% | **+16.7%** |

| **任务完成率 (200 step 内)** | 200 步内完成（不论对错）的比例 | 62.1% | 84.1% | **+22%** |

| **任务完成率 (纯 GUI 对比)** | 纯 GUI vs 混合模态 | 62.1% | 80.3% | **+18.2%** |

| **任务完成效率** | 平均完成步数 | 45.2 步 | 33.0 步 | **-27%** (步数减少即效率提升) |

  

**指标解读**：

- **成功率 +16.7%**：混合模态让模型能更准确地选择工具,减少 GUI 操作失误

- **完成率 +22%**：混合模态步数少,200 步内能完成更多任务

- **效率 +27%**：能用工具一步完成的就不用 GUI 十步,平均步数大幅减少

  

### **3.6、200 step 内完成的指标为什么这么设计？**

  

**原因**：

1. **防止无限循环**：模型可能陷入"点击-失败-再点击"的死循环,必须有上限。

2. **业务可接受性**：200 步约对应 10-15 分钟执行时间,是用户可接受的最大等待时长。

3. **成本控制**：每步消耗 LLM Token,200 步是成本上限。

4. **业界参考**：Anthropic Computer Use 评测也用类似 step 上限。

  

**指标分离的意义**：

- **成功率**衡量"对错"（质量）

- **完成率**衡量"能否在合理步数内完成"（效率 + 可行性）

- 两者分离能更细粒度诊断问题：如某模型完成率高但成功率低 → 模型能跑完但跑错了;反之 → 模型准但慢。

  

### **3.7、SkillBench 是什么？**

  

**SkillBench** 是团队内部的多工具调用能力评测基准,用于评估 CUA 模型在混合模态任务上的表现。

  

**特点**：

- 1000 道测试题（与训练集严格不重叠）

- 每道题有标准答案 + 多种正确路径

- 覆盖 5 大任务类型 × 3 难度等级

- 评测方式：沙箱环境实际执行 + 自动评分（基于最终状态对比）

  

**评分维度**：

1. **任务正确性**（40%）：最终状态是否符合预期

2. **工具选择合理性**（25%）：是否选了最合适的 Skill/MCP

3. **步骤效率**（20%）：步数是否接近最优

4. **错误恢复能力**（15%）：遇到异常时是否能恢复

  

---

  

## **4、Visual2Code 数据合成产线怎么设计的？**

  

这是项目的第二条主线,我搭建了"**分类标签树 + Multi-Agent 生成代码 + 质量筛选准入 + COT 合成**"四阶段产线。

  

### **4.1、什么是 chart2code 任务？**

  

**chart2code**：给定一张图表图片,生成可运行且视觉还原的代码。

  

```

输入: 一张柱状图图片

    ↓

输出:

```python

import matplotlib.pyplot as plt

categories = ['A', 'B', 'C', 'D']

values = [23, 45, 56, 78]

plt.bar(categories, values, color='#4C72B0')

plt.title('Sales Data')

plt.xlabel('Category')

plt.ylabel('Value')

plt.show()

```

```

  

**应用场景**：

1. **科研绘图复现**：看到论文里的图表,想用代码复现

2. **设计稿转代码**：设计师给的图表原型 → 前端代码

3. **数据可视化自动化**：用户提供样图 → 生成可定制代码

  

**核心挑战**：

1. **图表类型多**：138 种图表类型,每种生成逻辑不同

2. **技术栈多样**：matplotlib/seaborn/plotly/ECharts/D3/ggplot2 等 9 种

3. **风格细节多**：配色/字体/网格/标注/图例等 17 种风格元素

4. **COT 缺失**：模型直接图→代码,缺少分析过程

  

### **4.2、分类标签树怎么设计的？**

  

四维标签体系确保数据覆盖全面且均衡：

  

```

分类标签树

├── 维度1: 图表类型 (138 种)

│   ├── 基础图表 (20): 柱状图/折线图/饼图/散点图/面积图...

│   ├── 统计图表 (35): 箱线图/小提琴图/热力图/雷达图...

│   ├── 关系图表 (25): 桑基图/和弦图/网络图/树状图...

│   ├── 地理图表 (15): 地图/热力地图/气泡地图...

│   ├── 科研图表 (28): Circos图/Monte Carlo图/基因表达图...

│   └── 组合图表 (15): 双Y轴/子图阵列/嵌套图...

│

├── 维度2: 技术栈 (9 种)

│   ├── Python: matplotlib / seaborn / plotly

│   ├── JavaScript: ECharts / D3.js / Chart.js

│   ├── R: ggplot2

│   └── 通用: SVG / Canvas

│

├── 维度3: 风格元素 (17 种)

│   ├── 配色: 暗色/亮色/学术/商务/渐变

│   ├── 字体: 衬线/无衬线/等宽

│   ├── 网格: 有/无/淡

│   ├── 标注: 数据标签/注释/箭头

│   ├── 图例: 顶部/底部/右侧/无

│   └── 其他: 标题/坐标轴/背景色/边框...

│

└── 维度4: 任务意图 (2 类)

    ├── 意图A: 精确复现 (像素级还原原图)

    └── 意图B: 风格化重绘 (保留数据,允许风格变化)

```

  

**标签树的价值**：

1. **覆盖度量化**：可计算"已覆盖组合数 / 总组合数"（138 × 9 × 17 × 2 = 42,228 种组合）

2. **均衡采样**：避免某类图表过多,确保低频图表（如 Circos 图）也有足够样本

3. **难度分级**：组合越罕见难度越高,指导难度拒采

4. **Bad Case 定位**：如"热力图 + seaborn + 暗色"组合 Bad Case 多 → 针对性补数据

  

### **4.3、9 种技术栈是什么？为什么选这 9 种？**

  

| 技术栈 | 语言 | 适用场景 | 选择理由 |

|--------|------|---------|---------|

| **matplotlib** | Python | 通用科研绘图 | Python 数据科学生态最基础 |

| **seaborn** | Python | 统计图表 | 基于 matplotlib,统计图更简洁 |

| **plotly** | Python | 交互式图表 | Web 交互场景 |

| **ECharts** | JavaScript | 商业可视化 | 国内最流行的前端图表库 |

| **D3.js** | JavaScript | 自定义可视化 | 灵活度最高,复杂图表必备 |

| **Chart.js** | JavaScript | 轻量图表 | 上手快,中小企业常用 |

| **ggplot2** | R | 统计学术绘图 | 学术界（生物/医学）常用 |

| **SVG** | 通用 | 静态矢量图 | 底层格式,无依赖 |

| **Canvas** | 通用 | 高性能绘图 | 游戏类/大规模数据可视化 |

  

**覆盖逻辑**：Python（数据科学）+ JavaScript（前端）+ R（学术）+ 通用底层（SVG/Canvas）,覆盖 95% 的真实使用场景。

  

### **4.4、Multi-Agent 生成代码的架构怎么设计？**

  

三个 Agent 协作的代码生成产线：
数据模拟专家、代码编写专家、风格优化专家

数据模拟专家根据选用的图表类型、技术栈、风格元素和技术类型等分类，给出绘图的关键prompt、所有指标的数据、布局安排等

代码编写专家根据提供的提示词描述，给出图表的代码实现

风格优化专家是VLM模型，从视觉的角度评判给出的图表是否存在 文字/标题重叠、中文渲染失败、文字展示不全等

  

```



所有生成代码

    ↓

[Level 1: 代码可执行性] (自动)

    沙箱环境执行代码,捕获异常

    ├── 执行成功 → 进入 Level 2

    └── 执行失败 → 剔除 (或送回 Refiner)

    ↓

[Level 2: 视觉相似度] (自动)

    对比生成图与原图的视觉相似度

    ├── SSIM > 0.7 → 进入 Level 3

    └── SSIM ≤ 0.7 → 剔除

    ↓

[Level 3: 人工抽检] (人工)

    随机抽 10%,人工评估 GSB

    ├── G/S 占比 > 80% → 准入

    └── G/S 占比 ≤ 80% → 整批退回

```

  

**视觉相似度算法**：

- **SSIM（Structural Similarity Index）**：结构相似性,衡量两张图的感知差异

- **PSNR（Peak Signal-to-Noise Ratio）**：峰值信噪比

- **LPIPS（Learned Perceptual Image Patch Similarity）**：基于深度学习的感知相似度

- 综合分 = 0.4*SSIM + 0.3*PSNR(归一化) + 0.3*(1-LPIPS)

  

**为什么用 SSIM > 0.7 而不是更高？**

- 0.7 平衡了质量与产量（要求太高质量会大幅降低数据量）

- 0.7 已能保证视觉上"看起来像"

- 后续 RL 阶段会用更严格的 LPIPS 进一步筛选

  

### **4.6、COT 合成怎么做？**

对通过质量筛选的样本,反向生成 Chain-of-Thought：


**COT 结构**：

```

输入: 图表图片

COT:

1. 图表类型识别: 这是一张分组柱状图,有4个分组,每组3个柱子

2. 数据提取:

   - 分组: ['Q1', 'Q2', 'Q3', 'Q4']

   - 系列: ['Product A', 'Product B', 'Product C']

   - 数据: [[23, 45, 56], [34, 56, 67], ...]

3. 风格分析:

   - 配色: 使用了蓝色系渐变

   - 网格: 有淡灰色横向网格线

   - 图例: 位于图表右侧

4. 技术栈选择: 使用 matplotlib (适合静态科研绘图)

5. 代码结构规划:

   - 创建 figure 和 axes

   - 绘制分组柱状图 (plt.bar)

   - 设置配色 (colormap)

   - 添加网格和图例

   - 设置标签和标题

6. 代码生成: (实际代码)

输出: 可执行代码

```

  

**COT 生成方式**：

1. **GPT-4 反向生成**：用强模型对（图, 代码）对反向生成 COT,作为训练信号

2. **规则模板填充**：对常见图表类型用模板生成 COT

3. **人工校验**：抽 5% 人工检查 COT 质量

  

**COT 的价值**：

- 让模型学会"先分析再生成"的推理过程,而非端到端黑盒

- 复杂图表上 COT 训练的模型比无 COT 模型准确率高 15%+

- 可解释性增强,便于 Bad Case 归因

  

### **4.7、难度拒采是什么？**

  

**难度拒采（Difficulty-based Rejection）**：根据任务难度对数据分级,拒绝采集过难或过易的样本。

  

**难度评估维度**：

1. **图表复杂度**：元素数量（数据点 / 系列 / 分组数）

2. **技术栈稀缺度**：罕见技术栈（如 D3.js）难度高

3. **风格元素数量**：17 种风格元素组合越多越难

4. **视觉相似度达标难度**：SSIM 难以超过 0.7 的样本

  

**分级策略**：

- **Easy**（接受）：基础图表 + 主流技术栈 + 简单风格 → 大量采集

- **Medium**（接受）：中等复杂度 → 适量采集

- **Hard**（选择性接受）：复杂图表 + 稀有技术栈 → 少量采集,确保覆盖

- **Too Hard**（拒绝）：极端复杂（如 Circos + D3.js + 渐变 + 动画）→ 拒绝,避免噪声数据

  

**为什么拒绝过难样本？**

1. **质量风险**：过难样本生成的代码质量差,会污染训练数据

2. **成本不合理**：过难样本 Multi-Agent 产线需多轮优化,成本高

3. **边际效益低**：少数极端样本对模型整体能力提升有限

  

### **4.8、55 万条数据怎么交付的？**

  

**数据构成**：

  

| 数据类型 | 数量 | 用途 |

|---------|------|------|

| **SFT 数据（含 COT）** | 35 万 | 监督微调 |

| **SFT 数据（不含 COT）** | 10 万 | 直接输入输出对 |

| **RL 偏好对** | 10 万 | 强化学习（chosen/rejected 对） |

  

**交付流程**：

1. **数据生产**：Multi-Agent 产线 7×24 小时运行

2. **质量筛选**：三级筛选,通过率约 60%

3. **COT 合成**：对通过筛选的样本反向生成 COT

4. **难度分级**：标注 easy/medium/hard

5. **格式标准化**：统一 JSONL 格式,字段对齐训练框架

6. **数据审计**：人工抽检 5% + 自动去重 + 数据污染检测

7. **版本管理**：每次交付打版本号,可回滚

  

**数据格式示例（SFT）**：

```json

{

  "id": "v2code_000001",

  "image_url": "s3://bucket/charts/bar_001.png",

  "instruction": "请根据图表图片生成 matplotlib 代码",

  "cot": "1.图表类型:分组柱状图... 2.数据提取:... 3.风格分析:...",

  "output": "import matplotlib.pyplot as plt\n...",

  "metadata": {

    "chart_type": "grouped_bar",

    "tech_stack": "matplotlib",

    "style": ["blue_palette", "light_grid", "right_legend"],

    "difficulty": "medium",

    "ssim": 0.85

  }

}

```

  

**数据格式示例（RL 偏好对）**：

```json

{

  "id": "v2code_rl_000001",

  "image_url": "s3://bucket/charts/heatmap_002.png",

  "prompt": "生成 seaborn 热力图代码",

  "chosen": "(高质量代码,SSIM=0.92)",

  "rejected": "(低质量代码,SSIM=0.45)",

  "reason": "chosen 配色正确,rejected 配色错误"

}

```

  

---

  

## **5、自动化评测报告 SKILL 是什么？**

  

这是我设计的**第二大贡献**,用于 Bad Case 归因分析,指导数据合成方向。

  

### **5.1、评测报告 SKILL 的工作流程**

  

```

输入:

  - 人评 GSB 结果 (Good/Same/Bad + 理由)

  - 模型 Trace (生成过程)

  - 代码产物 (生成的代码)

    ↓

[SKILL 执行]

  1. 数据聚合: 按 chart_type × tech_stack × style 维度统计 Bad Case 分布

  2. 归因分析: 对每个 Bad Case 分类根因

  3. 趋势分析: 对比历史评测,识别退化/提升点

  4. 建议生成: 输出下一步数据合成建议

    ↓

输出: 结构化评测报告 (Markdown + JSON)

```

  

### **5.2、GSB 评测是什么？**

  

**GSB（Good/Same/Bad）** 是人工评测的相对评分方法：

- **Good**：比基线模型好

- **Same**：与基线模型相当

- **Bad**：比基线模型差

  

**评测流程**：

1. 同一输入给两个模型（基线 + 实验）生成代码

2. 人工对比两个输出,给出 GSB 判定 + 理由

3. 统计 GSB 分布,计算 Win Rate = Good / (Good + Bad)

  

**为什么用 GSB 而不是绝对评分？**

- 绝对评分主观性强,不同评测员标准不一

- 相对评分（A vs B）更容易判断

- GSB 是业界主流（OpenAI/Anthropic 都用）

  

### **5.3、Bad Case 归因分类**

  

我将 Bad Case 分为 7 类根因：

  

| 根因类别 | 描述 | 典型例子 | 改进方向 |

|---------|------|---------|---------|

| **图表类型误识别** | 模型看错图表类型 | 把堆叠柱状图看成分组柱状图 | 增加该图表类型训练数据 |

| **数据提取错误** | 从图中提取的数值错 | 把 23 看成 28 | 增强数值识别能力 |

| **技术栈不熟悉** | 代码语法错 | ECharts 配置项用错 | 增加该技术栈数据 |

| **风格元素遗漏** | 忽略配色/网格等 | 忘了加图例 | 风格元素数据补强 |

| **代码逻辑错** | 代码能跑但逻辑错 | 用了 plt.bar 而非 plt.barh | 代码逻辑训练数据 |

| **执行报错** | 代码无法运行 | 变量未定义/导入缺失 | 代码规范数据 |

| **视觉相似度低** | 能跑但视觉不像 | 配色/比例偏差 | 风格还原数据 |

  

### **5.4、评测报告输出示例**

  

```markdown

# Visual2Code 模型评测报告 v2.3

  

## 总体表现

- Win Rate: 62% (vs 竞品模型)

- GSB 分布: G=380, S=420, B=200

- 主要提升: 热力图/箱线图

- 主要退化: Circos 图

  

## Bad Case 归因 (Top 3)

1. **热力图 + seaborn + 暗色风格** (35 个 Bad Case)

   - 根因: seaborn 的 dark_palette 使用错误

   - 建议: 增加 seaborn 暗色风格数据 500 条

  

2. **Circos 图 + D3.js** (28 个 Bad Case)

   - 根因: D3.js 路径计算错误

   - 建议: 增加 Circos + D3.js 数据 300 条

  

3. **双 Y 轴 + matplotlib** (22 个 Bad Case)

   - 根因: twinx() 使用不当

   - 建议: 增加双 Y 轴数据 200 条

  

## 下一步数据合成建议

- 优先级 1: 热力图 + seaborn + 暗色风格 (500 条)

- 优先级 2: Circos + D3.js (300 条)

- 优先级 3: 双 Y 轴 + matplotlib (200 条)

```

  

### **5.5、为什么用 SKILL 而不是普通脚本？**

  

将评测报告封装为 SKILL 的好处：

1. **可复用**：其他团队可直接调用此 SKILL 生成评测报告

2. **可组合**：可与其他 SKILL（如数据合成 SKILL）串联,形成闭环

3. **可演化**：SKILL 内部逻辑可随业务迭代,接口不变

4. **标准化**：符合团队 SKILL 规范,易于接入 Agent 体系

  

---

  

## **6、数据合成相关八股**

  

### **6.1、SFT vs RL 数据的区别？**

  

| 维度 | SFT 数据 | RL 数据 |

|------|---------|---------|

| **格式** | instruction + input + output | prompt + chosen + rejected |

| **作用** | 教模型"怎么做" | 教模型"什么更好" |

| **数量** | 通常 10 万 - 100 万条 | 通常 1 万 - 10 万对 |

| **质量** | 必须高质量（教师模型/人工） | 偏好对,允许 chosen 不完美 |

| **训练方式** | 监督学习（交叉熵） | 强化学习（PPO/DPO/GRPO） |

| **成本** | 高（需高质量输出） | 中（只需相对好坏） |

  

**在本项目中**：

- SFT 数据 45 万（含 COT 35 万 + 不含 COT 10 万）

- RL 数据 10 万（偏好对）

  

### **6.2、数据合成的质量怎么保证？**

  

**五层质量保证体系**：

  

1. **源头质量**：种子任务/图片来自可靠来源（开源数据集 + 专家构造）

2. **生成质量**：Multi-Agent 协作比单 Agent 质量高

3. **筛选质量**：三级筛选（可执行 + 视觉相似 + 人工抽检）

4. **去重质量**：SimHash + 语义去重,避免数据冗余

5. **审计质量**：数据污染检测（避免测试集泄露）+ 人工抽检

  

### **6.3、数据污染（Data Contamination）怎么检测？**

  

**数据污染**：训练数据包含了测试集的样本,导致评测结果虚高。

  

**检测方法**：

1. **精确匹配**：训练样本与测试集样本的字符串完全一致

2. **近似匹配**：基于 n-gram 或 MinHash 的相似度检测

3. **语义匹配**：Embedding 相似度 > 阈值

4. **输出匹配**：训练样本的输出与测试集 ground truth 高度相似

  

**处理**：

- 发现污染样本立即从训练集剔除

- 重新生成等量干净样本补足

  

### **6.4、数据多样性的重要性？**

  

**数据多样性不足的后果**：

1. **过拟合**：模型记住特定模式,泛化差

2. **偏见**：对某些图表类型/技术栈过度偏好

3. **长尾能力弱**：低频图表（如 Circos）表现差

  

**多样性保证策略**：

1. **标签树均衡采样**：138 × 9 × 17 × 2 维度均衡

2. **难度分层**：easy/medium/hard 按比例（3:5:2）

3. **来源多样**：开源数据 + 人工构造 + 模型生成

4. **去重**：避免近似样本过多

  

### **6.5、什么是 Self-Instruct / Self-Improving？**

  

**Self-Instruct**：模型自己生成训练数据自我提升。

1. 用种子任务（175 条）作为 few-shot

2. 模型生成新任务 + 答案

3. 过滤低质量

4. 用新数据微调模型

5. 循环迭代

  

**优势**：无需人工标注,可大规模扩展

**劣势**：质量受限于模型自身能力,容易"自我强化偏见"

  

**在本项目中的应用**：Visual2Code 产线中,Refiner Agent 会基于 Reviewer 反馈自我优化代码,本质是 Self-Improving 思想。

  

### **6.6、什么是 Constitutional AI？**

  

**Constitutional AI（宪法 AI）**：Anthropic 提出,用一组"宪法"原则指导模型自我改进。

  

**流程**：

1. 模型生成输出

2. 用宪法原则（如"不应有偏见""不应危险"）自我审查

3. 模型生成修订版

4. 用修订版做 SFT/RL

  

**在本项目的启示**：Reviewer Agent 类似宪法审查,基于规则审查代码质量并指导 Refiner 优化。

  

---

  

## **7、多模态大模型相关八股**

  

### **7.1、VLM（Vision Language Model）的架构？**

  

主流 VLM 架构：

  

```

┌──────────────┐     ┌──────────────┐     ┌──────────────┐

│  Vision      │     │  Projection  │     │  LLM         │

│  Encoder     │────▶│  (Adapter)   │────▶│  (Decoder)   │

│  (CLIP/ViT)  │     │              │     │              │

└──────────────┘     └──────────────┘     └──────────────┘

```

  

- **Vision Encoder**：CLIP/ViT/SigLIP,把图片编码为视觉特征

- **Projection**：把视觉特征对齐到 LLM 的 embedding 空间（MLP/Q-Former/交叉注意力）

- **LLM**：理解图文混合输入,生成文本

  

**代表模型**：

- **CLIP**：对比学习,图文对齐

- **LLaVA**：CLIP + Vicuna,简单 MLP 投影

- **Qwen-VL**：ViT + Qwen,多分辨率支持

- **GPT-4V**：未公开架构

- **Gemini**：原生多模态（非拼接）

  

### **7.2、chart2code 任务对 VLM 的核心要求？**

  

1. **细粒度视觉理解**：识别图表中的数值、标签、颜色（不能只看大概）

2. **结构化输出**：生成符合语法的代码（而非自由文本）

3. **长程推理**：复杂图表需要多步分析（COT）

4. **技术栈知识**：了解不同库的 API 差异

5. **风格还原**：精确还原配色/字体/布局

  

### **7.3、FigureBench / ChartGalaxy 是什么？**

  

- **FigureBench**：团队内部图表理解与代码生成评测基准

- **ChartGalaxy**：开源/内部的图表多样性评测基准,覆盖广泛图表类型

  

这些基准用于：

1. 模型能力评估（Win Rate / 准确率 / 视觉相似度）

2. 与竞品对比（如 GPT-4V / Claude 3.5 Sonnet）

3. 版本迭代追踪（v1 → v2 → v3 性能曲线）

  

### **7.4、视觉相似度评估方法？**

  

| 方法 | 原理 | 优势 | 劣势 |

|------|------|------|------|

| **SSIM** | 结构相似性,衡量亮度/对比度/结构 | 符合人眼感知 | 对小变化敏感 |

| **PSNR** | 峰值信噪比 | 简单 | 不符合感知 |

| **LPIPS** | 深度学习特征距离 | 最符合人眼 | 需预训练模型 |

| **FID** | Fréchet Inception Distance | 适合批量评估 | 不适合单样本 |

  

本项目综合用 SSIM + LPIPS,平衡精度与成本。

  

---

  

## **8、Agent 工程相关八股**

  

### **8.1、Multi-Agent 协作的常见模式？**

  

1. **Pipeline（流水线）**：Agent 串行,前者输出为后者输入（本项目 Visual2Code 产线）

2. **Broadcast-Aggregate（广播聚合）**：Coordinator 广播任务,多 Agent 并行,结果聚合（会诊模式）

3. **Debate（辩论）**：多 Agent 互相辩论,收敛到共识

4. **Hierarchical（层次化）**：Manager Agent 分配子任务给 Worker Agent

5. **Market（市场）**：Agent 竞争任务,出价最高者执行

  

### **8.2、Agent 数据合成 vs 单 Agent 数据合成？**

  

| 维度 | 单 Agent | Multi-Agent |

|------|---------|-------------|

| **质量** | 一次性生成,易错 | 分工协作,质量高 |

| **成本** | 低 | 高（多倍 Token） |

| **速度** | 快 | 慢（多轮交互） |

| **可调试** | 黑盒 | 每环节可优化 |

| **适用** | 简单任务 | 高质量要求 |

  

**成本优化**：Multi-Agent 仅用于高质量种子数据,种子数据再用 Self-Instruct 扩展。

  

### **8.3、ReAct / CoT / ToT 的区别？**

  

- **CoT（Chain of Thought）**：纯推理链,Thought → Thought → Answer

- **ReAct**：推理 + 行动交替,Thought → Action → Observation → Thought → ...

- **ToT（Tree of Thought）**：树形搜索,多路径并行探索

  

**在 CUA-Next 中的应用**：模型用 ReAct 框架,每步 Thought（分析当前界面）→ Action（GUI 操作或 Skill 调用）→ Observation（新截图/工具返回）→ 下一轮 Thought。

  

### **8.4、Tool Calling / Function Calling 的原理？**

  

LLM 输出结构化 JSON 指定要调用的工具和参数：

  

```json

{

  "tool": "search_web",

  "parameters": {

    "query": "Python matplotlib tutorial"

  }

}

```

  

**实现方式**：

1. **Prompt 提示**：在系统提示词中列出可用工具的 JSON Schema

2. **Fine-tuning**：用工具调用数据微调模型（GPT-4 Turbo 等）

3. **Constrained Decoding**：用 JSON Schema 约束解码（Outlines/Guidance）

  

### **8.5、什么是 Agentic Workflow？**

  

**Agentic Workflow**：用多个 LLM 调用组合成复杂工作流,每个节点是 LLM 调用,通过条件分支/循环/并行组合。

  

代表框架：

- **LangGraph**：状态图编排

- **CrewAI**：角色协作

- **AutoGen**：对话式协作

- **Dify**：可视化工作流

  

本项目 Visual2Code 产线就是 Agentic Workflow 的典型应用。

  

---

  

## **9、工程化与生产相关**

  

### **9.1、数据产线的工程化挑战？**

  

1. **规模**：55 万条数据需 7×24 小时运行

2. **成本**：Multi-Agent 每条数据 3-5 次 LLM 调用,成本高

3. **监控**：需实时监控产线健康度（成功率/吞吐/成本）

4. **版本管理**：数据版本化,可回滚

5. **去重**：避免近似样本污染

6. **审计**：数据可追溯（来源/生成时间/质量分数）

  

### **9.2、如何优化数据产线成本？**

  

1. **模型分级**：高质量种子用 GPT-4,扩展数据用 Qwen2.5 等便宜模型

2. **缓存复用**：相同输入的输出缓存,避免重复调用

3. **批处理**：Batch API 比实时 API 便宜 50%

4. **早停策略**：Reviewer 第一轮就判废的不再送 Refiner

5. **并行化**：无依赖的样本并行生成

  

### **9.3、如何评估数据质量对模型的影响？**

  

**A/B 测试设计**：

1. **数据消融实验**：去掉某类数据,看模型性能下降多少

2. **数据剂量实验**：增加某类数据,看性能提升曲线

3. **数据质量对比**：高质量 vs 低质量数据,等量训练对比

  

**关键指标**：

- 训练损失收敛曲线

- 验证集准确率

- 下游 benchmark 表现

- Win Rate（vs 基线）

  

### **9.4、数据交付的 SLA 怎么定？**

  

| 指标 | SLA | 实际达成 |

|------|-----|---------|

| **数据量** | 10 万条/周 | 12 万条/周 |

| **通过率** | ≥60% | 65% |

| **人工抽检合格率** | ≥80% | 85% |

| **交付延迟** | ≤3 天 | 1.5 天 |

| **数据污染率** | ≤0.1% | 0.05% |

  

---

  

## **10、项目挑战与收获**

  

### **10.1、最大挑战**

  

1. **数据合成的质量-成本平衡**：Multi-Agent 质量高但成本高,需在 55 万条规模下控制成本。通过模型分级 + 早停 + 缓存优化,最终单条成本降低 40%。

  

2. **混合模态数据的复杂性**：GUI 操作和 Skills 调用的数据格式差异大,如何统一 Schema 是难题。设计了 HandoverManifest 类似的结构化协议,统一 GUI 动作和 Tool 调用的表示。

  

3. **Bad Case 归因的自动化**：人工归因不可扩展,需自动化。设计了基于规则 + LLM 的混合归因机制,7 类根因分类准确率达 82%。

  

4. **138 × 9 × 17 × 2 的覆盖度**：4.2 万种组合不可能全覆盖,需智能采样。基于重要性-难度-成本的三维采样策略,覆盖了 80% 的高价值组合。

  

5. **评测的客观性**：chart2code 评测主观性强（视觉相似度）。设计了 SSIM + LPIPS + 人工 GSB 的综合评测体系,平衡客观性与感知一致性。

  

### **10.2、最大收获**

  

1. **数据驱动的模型优化思维**：不是直接改模型,而是通过构造数据间接驱动模型能力提升

2. **Multi-Agent 工程实践**：理解了多 Agent 协作的优势与成本,学会在质量与成本间权衡

3. **数据质量保证体系**：从源头到交付的五层质量保证,可复用到其他数据项目

4. **评测驱动迭代**：通过自动化评测报告 SKILL 形成数据-模型-评测闭环

5. **大规模数据工程**：55 万条数据的产线设计、监控、交付,积累了生产级数据工程经验

6. **前沿技术视野**：CUA、MCP、VLM 等方向都是 2024-2025 最前沿,紧跟技术趋势

  

---

  

## **11、与竞品对比**

  

### **11.1、CUA-Next vs Anthropic Computer Use**

  

| 维度 | Anthropic CU | CUA-Next |

|------|-------------|----------|

| **能力** | 纯 GUI | GUI + Skills/MCP 混合 |

| **效率** | 慢（纯视觉） | 快（能用工具就不用 GUI） |

| **覆盖** | 任意应用 | 任意应用 + 有 API 的应用更快 |

| **架构** | 单模型 | 混合模态模型 |

  

### **11.2、Visual2Code vs GPT-4V chart2code**

  

| 维度 | GPT-4V | Visual2Code 优化后 |

|------|--------|-------------------|

| **图表类型覆盖** | 主流 20+ 种 | 138 种 |

| **技术栈** | 主流 3-5 种 | 9 种 |

| **风格还原** | 一般 | 精确（17 种风格元素） |

| **COT 推理** | 无 | 有 |

| **Win Rate** | 基线 | 62% (vs 基线) |

  

---

  

## **12、未来优化方向**

  

1. **CUA-Next**：

   - 移动端 CUA（Android/iOS）

   - 跨应用工作流（如从邮件复制内容到文档）

   - 长程任务规划（>100 步）

   - 错误恢复能力增强

  

2. **Visual2Code**：

   - 多模态输入（图 + 文字描述）

   - 交互式代码生成（用户反馈迭代）

   - 3D 可视化代码生成

   - 动态图表（动画/交互）

  

3. **数据产线**：

   - Active Learning（主动学习,优先标注高价值样本）

   - 数据飞轮（模型生成 → 评测 → 补数据 → 模型迭代闭环）

   - 合成数据质量自动评估

  

---

  

## **总结**

  

本项目作为**字节跳动基础大模型团队在 GUI 方向的能力建设**,核心价值在于：

  

1. **混合模态创新**：CUA-Next 的 GUI + Skills/MCP 混合模态,效率提升 27%,是 CUA 方向的重要探索

2. **数据产线工程化**：Visual2Code 的四阶段产线 + 三级筛选 + COT 合成,55 万条高质量数据交付

3. **评测驱动闭环**：自动化评测报告 SKILL 实现数据-模型-评测闭环,指导迭代方向

4. **前沿技术落地**：CUA、MCP、VLM、Multi-Agent 等前沿方向的工程化实践

5. **数据规模与质量并重**：55 万条数据 + 严格质量保证,支撑模型在基准上持续提升并超越竞品

  

这个项目让我深入理解了**数据驱动的大模型能力建设方法论**,以及**大规模数据工程的实践经验**,是大模型数据工程方向的核心能力体现。

  

---