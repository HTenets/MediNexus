# Production environment (self-hosted via Docker + nginx)
# REST API and WebSocket are served from the same host and reverse-proxied
# by nginx (/api/ and /ws/ -> backend). Leave NEXT_PUBLIC_WS_URL unset so the
# frontend auto-connects to the current host.
# NEXT_PUBLIC_WS_URL=
