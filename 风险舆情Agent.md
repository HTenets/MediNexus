好的，我将严格按照您提供的《股票投资顾问 Agent》项目文档的结构、内容深度、技术细节和问答逻辑，为您生成一份全新的《**热点事件舆论追踪与风险研判多 Agent 系统**》项目面试文档。

本系统的核心目标从“个股投资”转变为“宏观/行业/企业级热点事件的风险感知与影响评估”，但其底层架构、数据处理范式、模型训练策略和系统工程思想高度一致。以下是第一部分：**自我介绍、简历写法、项目描述**。

---

# **热点事件舆论追踪与风险研判多 Agent 系统**

# **自我介绍**

面试官您好，我叫XXX，本科毕业于东南大学，硕士就读于中国传媒大学计算机与网络空间安全学院，在校期间我曾多次获得过奖学金，我的研究方向是 AIGC 图片检测，有一篇以 xxx 为主题的论文在投 CVPR。

此外，我还有一段实习经历，是在 xxx 新闻科技公司做大模型算法实习生，主要参与的项目是**舆论追踪与风险研判多 Agent 系统**，是基于 MCP 架构的多 Agent 系统，可以对突发公共事件、行业政策变动或企业危机进行实时舆情监控、情感倾向判断、潜在风险量化及影响范围推演。我负责其中**多源异构新闻数据的工具化接入、事件分析工作流的 LangGraph 编排，以及情感与风险小模型的 LoRA 微调与部署优化**。

以上是我的自我介绍，需要我给您详细介绍一下这个 Agent 项目吗？

---

# **简历写法**

**热点事件舆论追踪与风险研判多 Agent 系统** | xxx 新闻科技公司 大模型算法实习生 2025.02 – 2025.07

**项目背景**  
公司希望利用大模型为新闻机构、政府机构及企业提供对突发热点事件（如政策发布、安全事故、舆情危机）的实时风险感知能力。初版方案采用通用大模型 + 网络爬虫，存在**信息碎片化、风险识别滞后、影响分析浅层化**等问题，难以支撑高时效性的决策需求。

**解决方案**  
1. **数据工具化**：基于 MCP 协议封装主流新闻、社交媒体、监管公告等 API，统一事件 Schema，确保多源数据可对齐、可追溯。  
2. **ReAct 框架 + 多 Agent 协作**：基于 LangGraph 和 ReAct 实现四个分析 Agent（**事件识别、情感研判、风险量化、影响推演**），支持自主调用 MCP 工具获取最新动态；由总结 Agent 汇总结果，生成包含**事件摘要、情感极性、风险等级、潜在影响行业/企业列表**的结构化报告。  
3. **情感与风险小模型**：整理并标注约 12 万条热点事件相关文本，基于 Qwen3-8B LoRA 微调情感与风险预测模型（测试集准确率 90% / 87%），用于快速初筛与量化。  
4. **性能与质量优化**：设计评测体系，监控工具调用成功率、事件覆盖完整率、端到端时延；通过并行执行与缓存，将重大事件响应时延从 3 分钟+降至 70 秒内。  
5. **模型接入与评测**：参与新闻专用大模型（Fin-R1）接入与 A/B 评测，引入 “**事实一致性 + 风险敏感度**” 奖励机制，奖励较通用模型提升 4–6%。

**项目成果**  
1. 实现对全网热点事件（日均 5000+）的分钟级捕获与分析，工具调用成功率达 97%，事件要素抽取完整率超 98%。  
2. 报告生成效率提升约 2 倍，灰度测试中用户（风控部门）对风险预警及时性的满意度达 92%。  
3. 构建覆盖 200+ 高频事件类型的标签体系与 3000+ 条典型场景知识库，支持后续迭代与规则沉淀。

---

# **项目描述**

我在这家新闻科技公司实习时，参与了一个名为“**热点事件舆论追踪与风险研判多 Agent 系统**”的项目。公司的目标是利用大模型为用户、企业、政府提供对突发公共事件（如某地化工厂爆炸、央行发布新监管政策、某科技巨头遭遇数据泄露）的**实时风险感知与影响评估能力**。我负责其中数据源的工具化和多Agent工作流的搭建，还有模型的训练和微调。以上是我的自我介绍

之所以要做这个系统，是因为传统舆情监控依赖关键词告警和人工阅读，**响应慢、覆盖面窄、缺乏深度研判**。当一个热点事件爆发时，决策者需要快速知道：这是什么事件？公众情绪如何？风险有多高？会影响哪些行业或关联企业？现有工具无法自动回答这些问题。

公司最初尝试直接用通用大模型（如 GPT-4）配合网络爬虫抓取新闻，但很快暴露了几个核心问题：

1.  **数据杂乱无章**：不同媒体对同一事件的报道标题、时间、主体不一致，模型无法有效聚合信息，常将一个事件误判为多个。
2.  **分析浮于表面**：模型能复述事件经过，但无法**量化风险等级**或**推演二级影响**。例如，无法判断“某省出台房地产限购新政”对建材、家电、银行等上下游行业的连锁冲击。
3.  **输出不可靠**：模型可能虚构不存在的政策细节或夸大影响范围，产生“幻觉”，在新闻风控场景下后果严重。
4.  **时效性不足**：从事件发生到生成完整报告耗时超过 3 分钟，错过了最佳响应窗口。

针对这些问题，我们设计了一套基于 **MCP 协议 + 多 Agent 协同**的智能体架构。我主要负责**数据源的工具化封装**和**事件分析工作流的搭建**。

在服务端，我们使用 **FastMCP** 将包括澎湃新闻、微博热搜、政府公告网站等在内的 50+主流媒体 数据源 API 封装成标准化工具。每个工具返回的数据都遵循统一的 **事件 Schema**，包含 `event_id`（事件唯一ID）、`core_entity`（核心主体）、`event_type`（事件类型）、`publish_time`（发布时间）、`source_url`（来源链接）等字段，并加入字段校验与异常兜底逻辑，确保输入大模型的数据干净、结构化。

在客户端，我们基于 **LangGraph** 实现了四个专业 Agent 的协同工作流：

- **事件识别 Agent**：负责从海量流式数据中聚类、去重，识别出独立的热点事件，并提取核心要素（谁、何时、何地、何事）。
- **情感研判 Agent**：分析围绕该事件的舆论情感倾向（正面/负面/中性），捕捉社会恐慌或乐观情绪。
- **风险量化 Agent**：评估事件本身的不确定性、潜在损失规模及监管介入可能性，给出 1-5 级风险评分。
- **影响推演 Agent**：基于知识图谱和行业关联规则，推演该事件可能波及的上下游行业、供应链企业或竞品公司。

这四个 Agent 均基于 **ReAct 框架**实现，能自主决定何时调用 MCP 工具（如“调用微博热搜API获取最新讨论热度”、“调用企业知识图谱查询关联方”），并在内部完成推理。它们可以**并行执行**以提升效率，最终由一个**总结 Agent** 将所有分析结果整合成一份 Markdown 格式的结构化报告。

为了提升分析的专业性和准确性，我为每个 Agent 设计了严格的 **Prompt 模板**，明确规定其必须输出的字段和分析维度，强制模型遵循“先事实、后推论”的逻辑。

在项目中期，我们发现仅靠大模型对原始新闻进行分析存在显著效率瓶颈：
- 自媒体时代信息源高度碎片化，日均处理事件超 2,000 条；
- 其中约 **68% 为中低风险事件**（如常规人事变动、例行政策解读），其分析价值有限；
- 若对所有事件均调用专用大模型执行完整的风险评估与影响推演，不仅**GPU 资源消耗高**，且**端到端响应延迟难以满足分钟级 SLA**。
为此，我们引入一个轻量级**风险初筛模型**：
- 基于 **Qwen2-1.5B** 进行 SFT 微调，聚焦**高风险事件识别**（二分类：高风险 vs. 非高风险，其中“高风险”定义为人工标注风险等级 ≥4）；
- 优化推理 pipeline，实现 **P99 延迟 ≤ 50ms**（P99 延迟 = 99% 的请求完成时间都 ≤ 该值）（在 A10 GPU 上，含预处理+推理+后处理）；
- 在内部测试集（12 万条标注样本）上，该模型达到 **Recall@HighRisk = 96.2%**，**Precision = 78.5%**，**F2-score = 91.3%**（优先保障高风险事件不漏报）。

（自媒体时代）事件的来源太多，60%的事件为中低风险事件，重复调用大模型进行风险评估与影响推演，运行效率低，成本高。为此，我们SFT了一Qwen2-1.5B的过滤模型，确保 P99 延迟低于 50ms，将风险简化为2分类，现在，72% 的常规事件被它快速过滤直接归档，不在触发Fin-R1，只有 28% 的高风险事件才触发专用大模型（ **SFT + GRPO** 两阶段训练）进行深度分析，在测试集上， **Recall@HighRisk = 96.2%**，**Precision = 78.5%**，**F2-score = 91.3%**（优先保障高风险事件不漏报）

该设计带来三重收益：
1. **性能提升**：系统平均端到端响应时间从 **112 秒降至 63 秒**（降低 43.8%）；
2. **成本优化**：Fin-R1 日均调用量减少 **71%**，月度 GPU 成本下降约 **¥18,000**（按 A10 实例计）；
3. **质量增益**：由于大模型获得强先验引导，其在高风险事件上的**关键风险点覆盖完整率**从 82% 提升至 **86.1%**（+4.1 个百分点），幻觉率下降 2.3%。



在性能方面，我主导了系统的评测与优化。通过**并行化 Agent 执行**、**对高频事件类型结果进行缓存**、以及**在工具层增加智能重试与备用源切换**，我们将端到端响应时延从最初的 3 分钟以上压缩至 **70 秒以内**，满足了客户对“分钟级响应”的核心需求。同时，我们建立了完善的监控指标，包括**工具调用成功率（97%）**、**事件要素完整率（98%）** 和**运行故障率（<0.5%）**。

目前，该系统已能稳定处理日均 500+ 的热点事件，覆盖政策、社会、经济、企业四大类 200+ 子类型。在灰度测试中，风控部门用户对系统**风险预警的及时性和影响推演的全面性**给予了高度评价，满意度接近 92%。我们还基于用户反馈，沉淀了一个包含 300+ 典型事件分析案例的知识库，为未来的自动化规则生成和模型迭代奠定了坚实基础。

---

# **项目相关的问题**

## **1、**事件识别Agent的数据来源？**

我们的事件识别 Agent 依赖一个**多层级、多模态、高时效的异构信息采集网络**，日均处理原始文本约 **52 万条**，覆盖 **98% 以上**的中文热点事件首发或早期传播路径。为平衡权威性、时效性与覆盖面，我们对不同信源设定了动态权重策略，并通过 **MCP（Model Calling Protocol）协议**统一接入。具体构成如下：

| 数据源类型      | 代表平台/接口                           | 日均数据量         | 更新频率                | 主要作用                      | 典型事件覆盖贡献                                  |
| ---------- | --------------------------------- | ------------- | ------------------- | ------------------------- | ----------------------------------------- |
| 主流综合新闻媒体   | 财新、澎湃新闻、新华社、人民日报、第一财经等 15 家       | ≈18 万条        | 实时（API 推送 + 5 分钟轮询） | 提供结构化、高可信度的事件主干信息         | 70% 的政策类、安全事故类事件由其首发或确认                   |
| 社交媒体平台     | 微博热搜/话题、知乎热榜、抖音热点（通过开放平台 API）     | ≈25 万条        | 实时流式（<30 秒延迟）       | 捕捉公众情绪爆发点、草根爆料、早期预警信号     | 85% 的企业舆情危机、社会突发事件在微博/抖音早于新闻媒体 10–60 分钟出现 |
| 政府与监管机构官网  | 央行、证监会、应急管理部、国家市场监管总局等 20+ 部委官网   | ≈3.5 万条       | 定时抓取（每 10 分钟）       | 获取一手政策文件、处罚决定、事故通报，确保法律效力 | 100% 的监管处罚类事件、重大公共安全事件以官方通报为准             |
| 企业征信与工商数据库 | 企查查、天眼查（API）、国家企业信用信息公示系统         | ≈4.2 万条（按需调用） | 按事件触发调用             | 验证事件主体真实性、查询历史风险、股权关联     | 在影响推演阶段 90% 的关联企业识别依赖此数据                  |
| 国际信源（按需启用） | 路透社、彭博社（英文）、BBC 中文（通过 RSS + 翻译模型） | ≈1.3 万条       | 15 分钟轮询             | 覆盖跨境新闻、地缘政治、大宗商品等外溢性事件    | 涉及 A 股市场的美联储政策、国际制裁等事件 100% 依赖此源          |
- 社交媒体占比最高（≈48%），因其具备最强的**早期信号捕获能力**；
- 新闻媒体虽量少于社交，但**信息密度与可信度最高**，作为事件“锚点”；
- 政府信源虽量最小，但**不可替代**，用于最终事实校验。

所有这些数据源均通过 **MCP（Model Calling Protocol）协议**进行标准化封装。我们开发了统一的 **FastMCP 工具框架**，每个数据源对应一个独立的 MCP Tool，负责：
- 调用原始 API 或爬虫接口；
- 对返回的原始 HTML/JSON 进行清洗、去广告、提取正文；
- 提取关键字段并映射到统一的 **事件 Schema**（含 `event_id`, `core_entity`, `publish_time`, `source_type`, `raw_url` 等字段）；
- 执行字段校验（如时间格式、URL有效性）和异常处理（如重试、降级）。

这种设计使得上层 Agent 无需关心底层数据源的具体实现，只需声明“我需要关于‘某化工厂爆炸’的最新微博讨论”，系统便会自动路由到对应的微博 MCP Tool 并返回结构化结果。

---

### **1.1、数据是怎么处理的？**

核心目标是**去噪、去重、结构化、打标**。

1. **原始数据采集**：通过分布式爬虫集群每日抓取全网约 50 万篇新闻/帖子。
2. **文本清洗**：移除广告、无关链接、重复段落；统一编码；过滤低质量内容（如字数 < 50 的碎片信息）。
3. **事件聚类与去重**：这是最关键的一步。我们采用 **MinHash-LSH + SimHash 双重去重机制**（详见 1.2 节），将描述同一事件的不同报道聚合成一个“事件簇”，并从中抽取最具代表性的主报道作为该事件的锚点。

我们的数据采用JSONL格式，包含source（来源信息）、doc（文档内容）、labels（标注信息）三个核心字段，我们对网页内容做提取，进行Unicode归一化处理，重点解决重复内容问题。由于很多新闻都是对同一事件进行报道，报道内容大同小异但又不会完全相同，采用精确指纹（例如SHA-256）无法过滤到类似的新闻，我们采用了近似指纹，具体来说我们采用了一种联合阈值策略**，**设计了"标题相似度 + 正文重合度 + 语义相似度"的三重阈值策略，使用三种算法处理不同维度的相似性：
- **标题相似度计算**：使用编辑距离和余弦相似度计算，阈值设为0.8。这部分主要处理标题的文本相似性，能够识别完全相同的标题或高度相似的标题变体。
- **正文重合度计算**：使用MinHash算法估计的Jaccard相似度，阈值设为0.7。MinHash能够快速计算两个文档的特征集合重叠程度，特别适合处理长文本的相似性判断。
- **语义相似度计算**：使用SimHash算法 64 位指纹的汉明距离来计算语义层面的相似性，阈值设为3。SimHash能够捕捉文本的语义特征，即使表达方式不同，只要核心内容相似，也能被识别出来。
应用三重阈值：只有当标题相似度 > 0.8 且正文重合度 > 0.7 且 SimHash ≤ 3 时，才判定为重复内容。经过过滤后，剩余约10万条新闻。
除了解决新闻重复问题以外，还做了数据标准化，比如统一时间格式为ISO-8601，统一货币代码为ISO 4217标准，统一证券标识符，对股市中4000余家公司建立“别名库”（简称、旧称、品牌名、英文名），保证能匹配到标准全称**。**

**补充说明**
	使用 SimHash 计算新文本与缓存事件指纹的**汉明距离**（Hamming Distance）。若距离 ≤ 3（对应 64 位指纹下 Jaccard 相似度约 >0.9），则视为重复，直接丢弃。
	Jaccard 相似度 ≈ 1 - (汉明距离 / 64)，故汉明距离 ≤3 对应高相似。

---

### **1.2、介绍下MinHash-LSH和SimHash**

这两个都是**局部敏感哈希**（Locality-Sensitive Hashing, LSH）算法，用于高效近似计算海量文本间的相似度，是我们事件去重与聚类的核心技术。

- **MinHash**：
  - **原理**：基于 Jaccard 相似度。将文本表示为词项集合（shingle），通过多个随机哈希函数对集合进行最小值采样，生成固定长度的签名向量（MinHash Signature）。两个文本的 MinHash 签名越相似，其 Jaccard 相似度越高。
  - **优势**：对词序不敏感，适合判断“是否描述同一事件”（即使措辞不同）。
  - **在本系统中的应用**：用于**离线大规模事件聚类**。我们将每日 50 万篇新闻两两比较的 O(n²) 问题，通过 LSH 索引降至近似 O(n)。每个事件簇分配一个全局唯一的 `event_id`，该 ID 即由簇内主报道的 MinHash 值派生。

- **SimHash**：
  - **原理**：将文本映射为一个 64 位（或 128 位）的二进制指纹（fingerprint）。相似文本的指纹汉明距离（Hamming Distance）很小。
  - **优势**：计算极快，内存占用小，支持实时比对。
  - **在本系统中的应用**：用于**在线流式去重**。当一条新新闻到达时，立即计算其 SimHash 值，并与 Redis 中缓存的最近 1 小时内所有事件 SimHash 值进行汉明距离计算（阈值设为 3）。若距离 ≤ 3，则视为重复，直接丢弃。

**汉明距离**就是：对两个等长序列，逐位比较，有多少个位置不同

**Jaccard（雅卡尔系数 / Jaccard Index）**是一种用于度量**两个集合相似度**的经典指标，广泛应用于**文本相似度、去重、聚类、信息检索**等场景。
1. 数学定义
给定两个集合 A 和 B，Jaccard 相似度定义为：
$$
\mathrm{Jaccard}(A, B) = \frac{|A \cap B|}{|A \cup B|}
$$

$$
|A \cap B|：交集大小（共有元素）
$$
$$
|A \cup B|：并集大小（所有不重复元素）
$$

**取值范围**：

$$
0 \le \mathrm{Jaccard}(A,B) \le 1
$$


两者结合：**MinHash-LSH 保证离线聚类的准确性，SimHash 保证在线处理的效率**，形成互补。

原文表述
**MinHash-LSH算法：**MinHash是一种局部敏感哈希算法，能够快速估计两个文档的Jaccard相似度。我们将每篇新闻分解为k-gram特征集合（k=3），然后通过多个哈希函数生成MinHash签名。具体实现中，我们使用128个哈希函数，每个函数生成一个MinHash值，最终形成128维的MinHash向量。

K-gram举例：（发生爆炸，3-gram，{发生爆，生爆炸}）
LSH（Locality Sensitive Hashing）用于加速相似度计算。我们将128维MinHash向量分成16个band，每个band包含8个值。只有当两个文档在至少一个band中的MinHash值完全相同时，才认为它们是候选相似对。这样可以将O(n²)的复杂度降低到O(n)，大大提高了大规模数据的处理效率。

**SimHash算法：**SimHash是一种基于特征权重的哈希算法，特别适合处理文本相似度。我们将新闻文本转换为TF-IDF特征向量，然后通过随机投影生成SimHash指纹。SimHash的优势在于能够捕捉语义相似性，即使文本表达方式不同，只要核心内容相似，也能被识别出来。


补充：去重的重要性：
1. 浪费资源和算力：学习重复的内容
2. 输出退化：可能突然出现一段Apache License 文本，或网页页脚模板

文档级去重：检测并删除完全相同或高度相似的整篇文章。常用技术是MinHash +LSH（局部敏感哈希），可以高效的处理数十亿篇文档
行级去重：在文档内部，删除大量重复出现的特定行（如" Cpoyright 2024 All rights reserved"、”点击这里查看更多“等），不是整段重复，而是作为模板嵌入在大量不同的页面中


---

### **1.3、数据是怎么打标的？**

数据标注是构建高质量情感与风险模型的基础。我们采用了 **“专家主导 + 规则辅助 + 质量校验”** 的三级标注流程。

考虑到10万条数据的规模，我们采用了"人工标注 + 大模型标注 + 一致性验证"的半自动标注流程：

**第一阶段：人工标注种子数据**
我们首先从10万条数据中随机抽样1000条（1%），由专业的新闻分析师进行人工标注。这1000条数据作为"种子数据"，用于制定标注规则，以及从中采样数据作为few-shot交给大模型。标注内容包括情感/风险得分以及打分理由。
**第二阶段：大模型批量标注**
基于1000条人工标注数据，设计了专门的Prompt模板，让多个大模型（GPT、Doubao、Deepseek）对剩余的9.9万条数据进行标注。在Prompt里写明了清晰的评分标准和示例，并要求模型给出标注理由。
**第三阶段：一致性验证和人工复审**

对于多个大模型标注不一致的样本（情感和风险各自大约5k条），由人工进行重新标注。然后又随机采样了1000条大模型标注一致的样本，由人工进行复核，计算Kappa系数为0.85，说明大模型和人类的评价标准高度一致。

1. **标注任务**：
   - **情感标签**：三分类（正面 / 负面 / 中性）。例如，“某新能源车企获政府补贴” → 正面；“某银行高管被查” → 负面。
   - **风险等级**：五级制（1=极低风险，2=低风险，3=中风险，4=高风险，5=极高风险）。评估维度包括：**潜在经济损失规模、监管介入可能性、公众恐慌程度、连锁反应广度**。


最终，我们构建了包含 **12 万条高质量标注样本**的数据集，其中负面/高风险样本占比约 65%，以匹配真实场景中的风险分布。

---

### **1.4、标注规则怎么制定的？**

标注规则并非凭空产生，而是基于 **舆情风险业务需求 + 历史案例复盘 + 专家共识** 三方面共同制定。

- **情感标注规则**：
  - **正面**：事件对市场、行业或特定企业产生明确利好（如政策支持、业绩超预期、技术突破）。
  - **负面**：事件引发损失、处罚、信任危机或不确定性（如安全事故、财务造假、监管处罚）。
  - **中性**：纯事实陈述，无明显情感倾向（如常规人事任命、例行发布会）。
  - **特殊处理**：对于“混合情感”事件（如“裁员但提升利润率”），要求标注**主导情感**，并在备注栏说明。

- **风险等级规则**（核心）：
风险分析：目标是衡量新闻揭示的未来不确定性和潜在损失程度。
- **1（极低风险）**：新闻几乎没有不确定性，未来发展高度确定，例如国家强力支持、确定性政策落地、公司基本面稳定。
- **2（低风险）**：存在轻微不确定性，但整体利好或中性。例如政策利好虽已公布但仍需执行落地，企业业绩虽稳健但可能受外部环境波动影响。
- **3（中等风险）**：存在一定不确定性，但影响有限；可能导致短期波动。例如个别高管事件、行业政策调整但不致命。
- **4（高风险）**：事件有重大不确定性，可能导致较大损失；例如监管调查、业绩大幅下滑预期，市场炒作。
- **5（极高风险）**：重大负面事件，未来高度不确定且潜在损失巨大，例如财务造假、退市风险、严重法律/合规问题。

  具体评估时，专家需回答四个问题并打分（1–5 分），最终取加权平均：
  1. **经济损失**：预计直接/间接损失金额（对上市公司则看市值影响）。
  2. **监管响应**：是否会引发调查、新规出台？
  3. **舆情烈度**：社交媒体讨论热度、主流媒体报道强度。
  4. **传导范围**：影响几个行业？多少家关联企业？

规则文档经风控委员会评审后固化，并随新事件类型动态更新。

---

### **1.6、Kappa系数怎么算？**

Kappa 系数（Cohen's Kappa）是我们评估**标注一致性**和**模型可靠性**的核心指标，用于衡量**排除随机猜测后的实际一致率**。

公式如下：

$$
\kappa = \frac{P_o - P_e}{1 - P_e}
$$

其中：
- $P_o$：观察到的实际一致率（Observed Agreement），即两位标注者（或模型与标注者）标签相同的样本比例。
- $P_e$：随机期望的一致率（Expected Agreement by Chance），基于各类别边际概率计算得出。

**在本项目中的应用**：
1. **标注质量评估**：在交叉校验阶段，计算两位专家对同一批样本标注的 Kappa 值。我们要求 Kappa ≥ 0.8（属“良好”一致性），否则需重新培训或修订规则。
2. **模型性能评估**：在测试集上，计算模型预测标签与专家标注之间的 Kappa，比单纯准确率更能反映模型在不平衡数据下的表现（因高风险样本少，准确率可能虚高）。

例如，情感三分类任务中，若模型准确率 90%，但 Kappa 仅 0.65，说明模型在“中性”类别上可能过度预测，需针对性优化。

### **1.7 事件识别agent和双重去重机制是否重复？**

在系统中，MinHash-LSH 和 SimHash 主要用于文本级去重，目的是消除转载和近重复内容，降低噪声；而事件识别 Agent 关注的是更高一层的语义问题，即不同文本是否在描述同一个现实世界事件。
事件识别 Agent 并不是简单做文本相似度或聚类，而是先抽取事件要素，如主体、行为、时间和地点，影响，后果，再基于逻辑一致性和时间演化关系判断是否合并事件。

因此，两者在层级和目标上是互补关系，而不是重复设计。
- **MinHash + LSH**：
    - 偏重词级 Jaccard 相似度
    - 对“转载、改写少量标题”的新闻非常有效
    - 适合**大规模粗筛**
- **SimHash**
    - 偏重语义整体指纹
    - 对句序变化、少量增删更鲁棒
    - 适合**精筛**

### **1.8那为什么不用 embedding + 聚类？**

embedding 更适合发现“语义相近”，但在舆情和风险场景中，我们更关心“是否指向同一事件”，这需要引入时间、主体和因果关系约束，仅靠向量距离是不够的。

---

## **2、轻量级风险初筛模型是怎么训练的？**

**事件识别、情感研判、风险量化等核心能力主要由大模型（Fin-R1）通过 Prompt Engineering 和 ReAct 推理实现。

但我们确实训练了两个**轻量级判风险初筛模型**作为辅助工具：
- **轻量级风险初筛模型**

它们的训练流程如下：

1. **数据准备**：使用 1.3 节所述的 12 万条标注数据，按 8:1:1 划分训练/验证/测试集。
	📌 **筛选标准**：仅保留包含明确主体、时间、结果的完整事件，剔除“传闻”“预测”“评论”类文本
2. **基座模型**：选用 **Qwen2-1.5B**，因其在中文理解和推理上表现优异，且支持高效微调。
3. **任务定义**：**二分类**（Binary Classification）
	- **正类（高风险）**：人工标注风险等级 ≥4（即“高风险”或“极高风险”）；
	- **负类（非高风险）**：风险等级 1–3
4. **标注规则**（节选）：
	- 出现以下任一情况即标为 **高风险**：
	    - 涉及人身伤亡（≥1 死亡或 ≥5 重伤）；
	    - 监管机构已立案调查；
	    - 主体为上市公司且事件可能引发股价异动（±5%）；
	    - 存在明显供应链中断风险（如芯片厂火灾）；
	    - 舆情讨论量 24h 内超 10 万条（微博+知乎+抖音）
5. **质量控制**：
    - 交叉校验率 20%，Kappa 系数 ≥ 0.81；
    - 每周召开标注对齐会，更新边界案例规则。
6. 最终分布：
	- **高风险样本**：38,200 条（占比 **30.9%**）
	- **非高风险样本**：85,300 条（占比 **69.1%**）
7. **微调方法**：采用 **LoRA**（Low-Rank Adaptation），仅训练低秩矩阵，冻结主干参数。这样既节省显存，又避免灾难性遗忘。
	 - **优化器**：AdamW，lr = 2e-4，warmup = 500 steps，Rank = 64，Alpha = 128；仅微调 attention 的 Q/V 投影矩阵；
	- **Batch Size**：256（8×A10，梯度累积）；
	- **损失函数**：**Focal Loss**（γ=2, α=0.75）
	    - 解决正负样本不平衡，强化对 hard example 的学习；
	- **训练时长**：约 6 小时（12 万样本，3 个 epoch）
8. **部署**：导出为 ONNX 格式，通过 Triton Inference Server 部署，供 Agent 实时调用。
9. **评估指标*

| 指标              | 值                 | 说明                      |
| --------------- | ----------------- | ----------------------- |
| Recall@HighRisk | 96.2%             | 高风险事件漏报率仅 3.8%，满足风控底线要求 |
| Precision       | 78.5%             | 误报率 21.5%，可接受（后续由大模型过滤） |
| F2-score        | 91.3%             | Fβ 中 β=2，更重视 recall     |
| Accuracy        | 84.7%             | 不作为主要指标（因分布不平衡）         |
| P99 推理延迟        | 48 ms             | A10 GPU，含预处理+后处理        |
| 模型大小            | 1.8 GB（FP16 ONNX） | 可单卡部署                   |
最终，它成功将大模型调用量降低 71%，同时将高风险事件漏报率控制在 4% 以内——这正是我们在效率与安全之间找到的最佳平衡点。

---

### **2.1 1.5B的小模型为什么还要RoRA微调，现有的12万条数据不能对进行全量微调吗**

- **经验法则**：全量微调通常需要 **每 1B 参数对应 10–50 万条高质量标注数据** 才能避免过拟合（参考 Hugging Face、Meta 等工业实践）
	- Qwen2-1.5B ≈ 15 亿参数；
	- 理论所需数据量：**150 万 – 750 万条**；
	- 实际仅有 **12.3 万条**，仅为下限的 **8%–20%**。
	- 
- 全量微调 vs LoRA：资源与效率对比

|维度|全量微调（Qwen2-1.5B）|LoRA（rank=64）|
|---|---|---|
|显存需求|≈ 28 GB（Adam 优化器 + 梯度 + 模型）|≈ 10 GB|
|训练硬件|需 A100 40G 或 2×A10|单卡 A10（24G）即可|
|训练时间|≈ 18 小时（8×A10）|≈ 6 小时（同配置）|
|存储成本|每次实验需保存完整 3GB 模型|仅保存 45MB LoRA 适配器|
|多任务扩展|每个任务需独立训练完整模型|多个 LoRA 适配器共享主干|
- LoRA 如何解决小数据下的泛化问题？
	 - LoRA 的核心优势在于：**冻结预训练知识，仅学习任务特异性增量**。
	- Qwen2-1.5B 在预训练阶段已学习海量中文文本，具备强大的语言理解与基础事实能力；
	- 风险判断任务本质是 **“在已有知识上叠加领域规则”**（如“伤亡→高风险”、“监管立案→高风险”）；
	- LoRA 通过低秩矩阵学习这些**轻量级规则映射**，无需重构整个语义空间。

>“您问得非常好。虽然 Qwen2-1.5B 只有 15 亿参数，但我们的 12 万条标注数据仍不足以支撑稳定全量微调——实验显示它在高风险样本上过拟合严重，Recall 波动超过 5 个百分点，这在风控场景是不可接受的。
>而 LoRA 通过冻结预训练知识，只学习‘风险判断’这一轻量级任务适配器，在单张 A10 上就能训练，最终 Recall@HighRisk 达到 96.2%，且非常稳定。
>更重要的是，LoRA 适配器仅 45MB，我们可以为不同客户（如银行 vs 政府）训练专属适配器，共享同一个主干模型，极大降低运维成本

### **2.1、难负样本都有哪些类别？**

“难负样本”指那些**容易被误判为高风险/负面，但实际并非如此**的样本。它们是提升模型鲁棒性的关键。我们识别出以下几类：

1. **反讽与幽默**：如“恭喜XX公司股价再创新低！”——表面祝贺，实为讽刺。情感模型易误判为正面。
2. **标题党**：如“惊！某银行要倒闭了？”——实际正文澄清为谣言。模型若只看标题会高估风险。
3. **正面事件的负面副作用**：如“新能源车补贴退坡”——对行业长期利好，但短期利空部分车企。情感和风险判断需区分视角。
4. **专业术语误读**：如“做空报告发布”——对被做空企业是负面，但对市场透明度是正面。模型需理解新闻语境。
5. **历史事件重提**：旧闻被翻出讨论，但事件已平息。若不加时间判断，会误判为新风险。

我们专门构建了一个 **“Hard Negative Mining” 数据集**，包含 5000+ 此类样本，用于对抗训练和模型校准。

---

### **2.2、难负样本训练是怎么做的？**

针对难负样本，我们采取了三种策略：

1. **数据增强**：在训练集中过采样难负样本，并对其做轻微扰动（如同义词替换、句式变换），提升泛化能力，专家构造的对抗样本（如标题党、反讽、模糊表述等难例）。
2. **对抗训练**：在损失函数中加入难负样本的梯度惩罚项，迫使模型在这些边界案例上更谨慎。
3. **集成校准**：在推理阶段，若小模型对某样本的预测置信度低（如 softmax 最大概率 500ms），自动降级为规则引擎（基于关键词匹配的风险初判），保证系统可用性。
4. **监控告警**：实时采集 QPS、P99 延迟、错误率等指标，异常时自动告警并切换备用实例。

经优化，单个模型实例可支撑 200+ QPS，P99 延迟 < 80ms。

---

### **2.3、舆情数据来源是哪里？**

本系统虽聚焦“热点事件”，但事件主体（如企业、行业）的**基本面、关联关系、历史风险**等新闻属性数据至关重要。我们主要从以下渠道获取：

- **商业新闻数据库**：万得（Wind）、Choice，用于获取上市公司公告、财务数据、行业分类。
- **企业征信平台**：企查查、天眼查 API，用于查询企业股权结构、司法风险、经营异常等。
- **监管公开数据**：证监会处罚公示、央行征信系统（脱敏后）、国家企业信用信息公示系统。
- **内部知识图谱**：公司自建的“企业-行业-政策”三元组图谱，包含 500 万+ 实体和 2000 万+ 关系。
- **企业征信数据**：通过企查查、天眼查 API 查询司法风险、经营异常等；
- **行业与供应链关系**：基于内部知识图谱（融合公开工商数据构建）；
- **上市公司公告**：通过交易所官网或开源接口获取，非依赖 Wind/Choice。

这些数据同样通过 **MCP 工具封装**，例如 `query_company_risk(company_name)`、`get_industry_chain(industry)`，供影响推演 Agent 调用。

---

## **3、工具调用失败2%是什么原因，怎么优化？**

工具调用成功率 98%，失败率约 2%。失败原因主要包括：

1. **第三方 API 限流或宕机**（~1.2%）：如微博 API 突发限流。
2. **网络波动或超时**（~0.5%）：跨地域调用延迟高。
3. **输入参数非法**（~0.2%）：如传入不存在的企业名。
4. **返回数据格式变更**（~0.1%）：对方 API 升级未通知。

**优化措施**：

- **智能重试机制**：对 5xx 错误自动重试 2 次，间隔指数退避。
- **备用数据源**：关键工具（如企业查询）配置主备两个数据源（企查查 + 天眼查），主源失败时自动切备源。
- **参数校验前置**：在调用前，用正则或缓存校验参数合法性（如企业名是否在名录中）。
- **Mock 降级**：对非核心工具，失败时返回 Mock 数据（如“暂无风险信息”），并记录日志供事后分析。
- **主动监控**：每日巡检各工具健康度，提前发现 API 变更。

**超时、重试、缓存**
1. **方案1：超时**
	**1. 问题描述**
	**不稳定表现**：
	- 响应慢：5-8秒（正常），>60秒（超时）
	**影响**：
	- 用户体验差（等待时间长）
	- 系统不可用（频繁超时）
	- 成本增加（重试次数多）
	**2. 方案1：超时控制**
	**适用场景**：
	- API偶尔超时（<5%的概率）
	- 不希望用户无限等待
	- 对实时性要求高
	**效果**：
	- 避免无限等待
	- 超时后快速失败
	- 可以配合重试机制

2. **方案2：重试机制**
	**指数退避策略**
	**为什么用指数退避？**
	- 避免频繁重试导致API限流
	- 给API恢复时间
	- 平衡响应时间和成功率
	**适用场景**：
	- API偶尔超时（5-10%的概率）
	- 对成功率要求高
	- 可以接受较长等待时间
	**效果**：
	- 重试成功率：约30%
	- 平均响应时间：从5-8秒增至6-10秒
	- API调用成功率提升
	
3. **方案3：缓存机制**
	**适用场景**：
	- 重复请求多（>20%的请求是重复的）
	- API调用成本高
	- 对实时性要求不高
	**效果**：
	- 缓存命中率：约20%
	- 响应时间：从5-8秒降至<10ms（缓存命中）
	- 成本降低20%
**5. 方案对比**

|方案|适用场景|优点|缺点|成本|
|---|---|---|---|---|
|**超时控制**|偶尔超时（<5%）|避免无限等待|不提高成功率|无|
|**重试机制**|偶尔超时（5-10%）|提高成功率|增加响应时间|低|
|**缓存机制**|重复请求多（>20%）|降低成本、提升速度|占用内存|中|

**8. 生产环境表现**
**监控指标**（2024年11月）：
- 超时率：从5%降至3%（重试后）
- 缓存命中率：20%
- API调用成功率：从95%提升至98%
- 平均响应时间：从5-8秒降至4-6秒
**成本优化**：
- API调用次数：降低20%（缓存）
- 总成本：降低约15%
**总结**：
- 超时控制：避免无限等待，适用场景少
- 重试机制：提高成功率，适用场景中等
- 缓存机制：降低成本，适用场景多
- 组合方案：效果最好，推荐使用

通过上述措施，我们将有效失败率（影响最终报告质量的）降至 0.3% 以下。

---
## **4、详细介绍一下每个Agent负责的工作**

本系统采用 **多专业 Agent 协同架构**，共包含 **4 个核心分析 Agent + 1 个总结 Agent**，均基于 **ReAct（Reasoning + Acting）框架**实现，并通过 **LangGraph** 编排执行逻辑。各 Agent 职责如下：

- **事件识别 Agent**（Event Identification Agent）  
  **职责**：从海量流式数据中识别出独立、完整的热点事件。  
  **工作流程**：  
  1. 接收来自 MCP 工具的原始新闻/帖子流；  
  2. 调用 SimHash 进行实时去重；  
  3. 对新事件触发 MinHash-LSH 聚类（若为离线批处理）；  
  4. 抽取核心要素：`事件ID`、`核心主体`、`发生时间`、`地点`、`事件类型`；  
  5. 输出结构化事件对象，并广播给其他 Agent。  
  **关键能力**：抗噪声、跨媒体事件融合、主报道选择。

- **情感研判 Agent**（Sentiment Analysis Agent）  
  **职责**：评估围绕该事件的公众及市场情绪倾向。  
  **工作流程**：  
  1. 接收事件对象；  
  2. 自主调用 MCP 工具获取相关社交媒体评论（如微博话题、知乎问答）；  
  3. 基于大模型（Fin-R1）进行深度推理，区分“表面情绪”与“深层态度”（如反讽）；  
  4. 输出：情感极性（正面/负面/中性）、情感强度（0–1）、关键情绪关键词。  

- **风险量化 Agent**（Risk Quantification Agent）  
  **职责**：对事件潜在危害进行结构化评估与打分。  
  **工作流程**：  
  1. 接收事件对象；  
  2. 调用企业征信工具（如企查查）查询主体历史风险；  
  3. 调用行业知识图谱判断事件所属行业敏感度；  
  4. 调用风险小模型获取初评等级；  
  5. 基于 Fin-R1 进行多维度推理，综合经济损失、监管响应、舆情烈度、传导范围四因子；  
  6. 输出：风险等级（1–5）、主要风险类型（如“合规风险”、“供应链中断”）、置信度。

- **影响推演 Agent**（Impact Inference Agent）  
  **职责**：预测事件可能波及的关联方与行业。  
  **工作流程**：  
  1. 接收事件对象及风险等级；  
  2. 调用企业知识图谱，查询核心主体的**上下游供应商、客户、竞品、控股子公司**；  
  3. 调用行业关联规则库（如“房地产政策 → 建材、家电、银行”）；  
  4. 利用 Fin-R1 的因果推理能力，生成影响路径（如“A工厂停产 → B车企缺芯 → C零部件商订单减少”）；  
  5. 输出：受影响行业列表、高风险关联企业 Top 5、影响程度预估（高/中/低）。

- **总结 Agent**（Summary Agent）  
  **职责**：整合所有分析结果，生成面向用户的最终报告。  
  **工作流程**：  
  1. 汇总前四个 Agent 的输出；  
  2. 按照预设模板（Markdown 格式）组织内容；  
  3. 强制校验事实一致性（如情感与风险是否逻辑自洽）；  
  4. 添加免责声明（如“本报告基于公开信息，不构成投资建议”）；  
  5. 输出结构化报告，并推送至用户终端或风控系统。

所有 Agent 均具备 **自主决策能力**：可根据中间结果决定是否需要调用更多工具（如发现主体是上市公司，则额外调用 Wind 获取股价异动）。

---

### **4.1、举个例子说明这几个Agent的关系**

以 **“某省突发化工厂爆炸事故”** 为例：

1. **事件识别 Agent** 首先从澎湃新闻、微博热搜、应急管理局公告中捕获多条信息，聚类为同一事件，提取要素：  
   - `核心主体`：XX化工有限公司  
   - `地点`：某省某市  
   - `时间`：2025-06-15 14:30  
   - `事件类型`：安全生产事故  

2. **情感研判 Agent** 调用微博 API 获取“#XX化工爆炸#”话题下 10 万条评论，经情感模型和大模型分析，判定为 **强烈负面情绪**（恐慌、愤怒），关键词包括“毒气泄漏”“孩子上学怎么办”。

3. **风险量化 Agent**：  
   - 调用企查查，发现该公司曾有 2 次环保处罚记录；  
   - 查询知识图谱，确认其为当地重点排污单位；  
   - 结合伤亡人数（初步通报 3 死 15 伤）和舆情烈度，给出 **风险等级 4**（高风险），主要风险类型为“公共安全+环境风险”。

4. **影响推演 Agent**：  
   - 查询知识图谱，发现该公司是某上市农药企业的核心原料供应商；  
   - 行业规则库提示：化工事故 → 化工板块股价波动 → 环保督查升级；  
   - 推演结果：  
     - 受影响行业：基础化工、农药、环保设备；  
     - 高风险关联企业：A 农药（代码：XXXX）、B 环保科技；  
     - 影响程度：高（因原料不可替代）。

5. **总结 Agent** 将以上信息整合为一份报告，包含事件摘要、情感分析、风险评级、影响企业列表，并标注“建议立即监控 A 农药股价及供应链公告”。

**关系总结**：  
- **并行协作**：情感、风险、影响三个 Agent 在事件识别完成后可并行启动，提升效率；  
- **信息依赖**：影响推演依赖风险等级和主体信息；情感与风险需交叉验证；  
- **统一出口**：所有结果由总结 Agent 统一格式化输出，确保一致性。

### 4.2 介绍一下整个系统的工作流
####  一、整体架构：流式为主，存储为辅

系统采用 **“流式事件驱动 + 按需持久化”** 架构：
- **热数据（<7 天）**：仅存于 **Kafka + Redis**，不落盘数据库；
- **冷数据（≥7 天）或高价值事件**：异步写入 **MongoDB**（用于回溯、审计、知识库构建）；
- **用户查询**：优先走实时流水线，**不依赖历史数据库**（除非明确要求查历史）。

> 📌 **设计原则**：  
> **“能实时算的，绝不查库；必须存的，异步写”** —— 保证分钟级响应 SLA。
#### 二、用户查询某新闻时的完整工作流程（以“查询某化工厂爆炸事件”为例）
![[tongyi-mermaid-2026-01-25-161604.png]]
##### 关键点说明：
1. **所有数据均为即时获取**：MCP 工具调用的是**当前最新 API**，不是查历史库；
2. **无中间数据库查询**：整个流程不读取 MongoDB，完全依赖实时工具；
3. **并行执行**：情感、风险、影响三个 Agent 可并行调用工具和模型；
4. **按需触发**：仅高风险事件才启动深度分析，低风险直接返回。
#### 三、哪些数据需要存数据库？为什么
|数据类型|存储位置|是否必需|用途|
|---|---|---|---|
|原始新闻/帖子|不存储（Kafka 保留 72h）|❌|仅用于实时处理|
|结构化事件对象（含 event_id、要素）|MongoDB（异步写入）|✅|1. 用户回溯查询  <br>2. 构建事件知识库  <br>3. 审计与合规|
|Agent 中间推理步骤|不存储|❌|仅调试时临时记录|
|最终报告|MongoDB + 对象存储（PDF/Markdown）|✅|用户历史查看、监管报送|
|MCP 工具调用日志|ELK 日志系统|✅|故障排查、成功率监控|
💡 **存储策略**：
- 写入 MongoDB 是**异步后台任务**，不影响主流程延迟；
- 仅当事件被用户查看或标记为“高价值”时，才强制持久化。
#### 四、后续还需要“数据处理”吗？

**分两种场景**：
#### 场景 1：**用户实时查询**（99% 的情况）
- **不需要后续处理**：所有分析基于即时数据完成，结果直接返回；
- **数据生命周期结束**：若未被标记为重要，72 小时后 Kafka 数据自动过期。
#### 场景 2：**系统自主发现重大事件**（如微博热搜 Top 3）
- **触发后续处理**：
    1. 自动持久化到 MongoDB；
    2. 推送至风控告警系统；
    3. 加入“典型事件库”，用于 RAG 检索；
    4. 样本进入标注队列，用于模型迭代。

> 📌 **核心区别**：  
> **用户驱动查询 = 纯实时、无存储**；  
> **系统主动发现 = 实时分析 + 异步沉淀**。

#### 五、各 Agent 如何配合？——协作机制详解
1. **主控 Agent（Orchestrator）**：
    - 负责 workflow 编排（LangGraph 实现）；
    - 决定是否启动深度分析（基于风险初筛结果）；
    - 汇总各 Agent 输出，校验一致性。
2. **事件识别 Agent**：
    - 是所有分析的**唯一入口**；
    - 输出标准化事件对象，供其他 Agent 共享。
3. **专业 Agent（情感/风险/影响）**：
    - **无状态**：每次调用都是独立请求；
    - **自治性**：可自主决定调用哪些 MCP 工具；
    - **并行执行**：由 LangGraph 配置为并发节点。
4. **总结 Agent**：
    - 强制格式化输出（Markdown/JSON）；
    - 添加免责声明；
    - 触发异步存储（如需要）。
> 🔗 **通信方式**：  
> 所有 Agent 通过 **LangGraph 的 State Graph** 共享上下文，**不通过数据库或消息队列传递中间结果**，避免 I/O 瓶颈。

#### 六、面试回答话术（体现架构思维）
> “我们的系统是典型的 **‘流式智能体’架构**：
> - 当用户查询一个事件时，**事件识别 Agent 会实时调用 MCP 工具获取最新信息**，而不是查历史数据库；
> - 所有后续分析（情感、风险、影响）都基于这些即时数据，由多个 Agent 并行完成；
> - **只有最终报告和高价值事件才会异步写入 MongoDB**，用于回溯和知识沉淀；
> - 整个过程平均耗时 63 秒，其中 95% 的时间花在外部 API 调用和大模型推理上，**数据库读写零参与**。
> 这种设计确保了系统既能满足分钟级响应的业务需求，又能通过异步存储积累长期价值。”

---

## **5、如何保证系统的安全性？特别是新闻数据的敏感性？**

系统安全是新闻级应用的生命线。我们从 **数据、模型、接口、合规** 四个层面构建防护体系。

- **数据安全**：  
  - 所有外部数据（新闻、社交媒体）均为**公开信息**，不涉及用户隐私；  
  - 内部新闻数据（如企业征信）通过 **API 网关鉴权**，仅返回脱敏字段（如“存在司法风险”而非具体案号）；  
  - 敏感中间结果（如企业关联图谱）**不落盘**，仅在内存中流转。

- **模型安全**：  
  - 大模型 Fin-R1 经过 **安全对齐训练**，禁止生成未证实的猜测或敏感结论（如“该公司将破产”）；  
  - 所有 Agent 的 Prompt 中强制加入 **安全约束**：“仅基于提供的事实进行分析，不得臆测”；  
  - 输出前经过 **事实一致性校验模块**，过滤幻觉内容。

- **接口安全**：  
  - MCP 工具调用采用 **OAuth 2.0 + IP 白名单**；  
  - 用户请求需携带 JWT Token，按角色控制访问权限（如普通用户看不到关联企业明细）。

- **合规性**：  
  - 系统通过 **等保三级认证**；  
  - 所有输出报告附带 **免责声明**，明确“不构成投资或决策建议”；  
  - 日志全量审计，保留 180 天，满足新闻监管要求。

---

### **6.1、具体规则有哪些**

安全规则内嵌于系统各环节：

1. **输入过滤规则**：禁止查询涉及国家领导人、军事、宗教等敏感关键词，自动拦截。
2. **输出过滤规则**：
   - 禁止出现“肯定性预测”（如“股价必跌”），只能用“可能”“值得关注”；
   - 风险等级 ≥4 的事件，必须附带“信息来源”和“数据时效”；
   - 不得披露非公开的企业财务细节。
3. **模型行为规则**：
   - 若工具调用失败，不得自行编造数据；
   - 情感分析不得使用煽动性词汇（如“血洗”“崩盘”）。
4. **审计规则**：所有 Agent 的工具调用记录、中间推理步骤、最终输出均写入审计日志，支持回溯。

---

### **6.2、怎么接入的新闻大模型**

我们接入的是公司自研的 **Fin-R1 新闻大模型**（基于 Deepseek R1 蒸馏 + SFT + GRPO 微调），接入方式如下：

1. **API 封装**：将 Fin-R1 部署为 gRPC 服务，提供 `generate()` 和 `embed()` 接口。
2. **Agent 集成**：在 LangGraph 的每个 Agent 节点中，通过统一的 **Model Client** 调用 Fin-R1。
3. **上下文管理**：为每次推理构造结构化 Prompt，包含：
   - 系统指令（含安全规则）；
   - 工具返回的结构化数据；
   - 历史对话（多轮场景）；
   - 输出格式约束（JSON Schema）。
4. **缓存与降级**：高频 Prompt 模板结果缓存；Fin-R1 不可用时，降级至 Qwen3-8B 开源模型。

---

## **7、具体指标相关**

### **7.1、关联影响预测准确率78%是怎么算出来的？**  
我们定义 **“关联影响预测准确率”** 为：在事件发生后 24 小时内，系统报告中列出的“高风险关联企业”，其是否出现显著市场反应（如股价波动 ≥3%、发布澄清公告、供应链中断声明）的比例。  
计算方式：
$$
\text{命中率} = \frac{\text{被验证的关联企业数}}{\text{系统报告的关联企业总数}}
$$

在 2025 年 Q2 的 500 起重大事件回测中，该指标达 **78.2%**。

---

### **7.2、数据一致率99%是什么意思？**

指 **同一事件在不同数据源中的核心要素（主体、时间、地点）保持一致的比例**。

例如，澎湃新闻、微博、政府公告对“化工厂爆炸”的报道，若主体均为“XX化工有限公司”，时间误差  用户：“刚才那个化工厂事件，会影响新能源车吗？”  
> 系统：调用影响推演 Agent 重新聚焦“新能源车供应链”，生成子报告。

技术实现：
- 使用 **LangGraph 的 Stateful Graph** 保存对话状态；
- 每轮用户输入作为新事件触发子工作流；
- 总结 Agent 合并历史与新分析，生成连贯回复。

所有中风险以上的均提供来源，并且预测输出内容与来源内容中的（主体、时间、地点等实体要素保持一致）

### 7.3 其他指标

> LangGraph 使用 from **agentevals**.trajectory.match import create_trajectory_match_evaluator 来进行评估
####  1. 数据层指标（衡量输入质量）

| 指标                | 定义                                | 目标值        | 为什么重要         |
| ----------------- | --------------------------------- | ---------- | ------------- |
| 1. 信源覆盖率          | 覆盖的主流媒体/社交平台占行业标准清单的比例            | ≥95%       | 避免因信源缺失导致事件漏报 |
| 2. 事件捕获延迟         | 从事件首次在网络出现 → 系统识别并生成 event_id 的时间 | P95 ≤ 8 分钟 | 决定能否实现“早期预警”  |
| 3. 数据覆盖完整率（前文已详述） | 单事件关键要素的捕获比例                      | ≥92%       | 保证分析基础信息不缺失   |
#### 2. 模型层指标（衡量 AI 能力）

|指标|定义|目标值|特别说明|
|---|---|---|---|
|4. 高风险事件召回率（Recall@HighRisk）|真实高风险事件中被初筛模型正确识别的比例|≥95%|风控底线指标，宁可误报不可漏报|
|5. 风险等级一致性|Fin-R1 输出风险等级 vs 专家标注的 Kappa 系数|≥0.78|衡量大模型判断是否稳定可靠|
|6. 影响推演准确率|推演出的关联企业中，被后续市场/公告验证的比例|≥78%|反映因果推理能力（见原 7.1 节）|
|7. 幻觉率|Fin-R1 生成内容中包含无依据事实的比例|≤3%|通过人工抽样或 NLI 模型检测|
注意：不用“整体准确率”，因为风险研判是**长尾、非均衡任务**，需分层评估。

#### 3. 系统层指标（衡量工程效能）
|指标|定义|目标值|业务意义|
|---|---|---|---|
|8. 端到端响应时间|用户提交查询 → 返回结构化报告的时间|P95 ≤ 90 秒|满足分钟级决策 SLA|
|9. 大模型调用节省率|因初筛模型过滤而避免的 Fin-R1 调用比例|≥70%|直接降低 GPU 成本|
|10. 系统可用性（SLA）|月度正常运行时间占比|≥99.5%|新闻/政企客户硬性要求|
#### 4. 业务层指标（衡量实际价值）
|指标|定义|测量方式|为什么关键|
|---|---|---|---|
|11. 关键风险点覆盖完整率|报告中包含的专家认为“必须提及”的风险点比例|专家抽样评估|直接决定报告是否可用（如你提到的 +4.1% 提升）|
|12. 用户采纳率|用户对系统建议采取实际行动的比例（如发起尽调、发布声明）|客户反馈 + 日志埋点|终极价值指标——AI 是否真正驱动了决策|
📌 示例：某银行风控部使用系统后，将“舆情响应平均时长”从 4 小时缩短至 45 分钟，即为业务价值体现。
#### 如何向面试官展示？

> “我们构建了一个四层评估体系：
> 
> - **数据层**看‘有没有’——比如事件是否在 8 分钟内被捕获；
> - **模型层**看‘准不准’——特别是高风险事件召回率必须 ≥95%；
> - **系统层**看‘快不快、省不省’——现在大模型调用量降了 71%；
> - **业务层**看‘有用没用’——最终以‘关键风险点覆盖完整率’和‘用户是否采纳’为金标准。
> 
> 比如最近一次迭代，我们通过优化 MCP 工具，将**数据覆盖完整率**从 89% 提到 92.7%，直接带动**风险点覆盖完整率**提升 4.1%，客户采纳率上升 12%。”

---

## **8、200+ 事件类型标签体系”和“3000+ 典型场景知识库**

### 1. 200+ 高频事件类型标签体系：如何构建？

#### 1. **初始种子来源（自下而上 + 自上而下结合）**

- **自下而上（数据驱动）**：
    - 对 **2023–2024 年 85 万条历史热点事件**进行无监督聚类（Sentence-BERT + HDBSCAN）；
    - 人工归纳出 **156 个初版事件簇**，如“环保处罚”“高管被查”“产品召回”等。
- **自上而下（业务驱动）**：
    - 参考 **新闻风控、ESG、舆情监测三大领域的标准分类**：
        - 证监会《上市公司信息披露管理办法》中的重大事件清单；
        - MSCI ESG 评级中的争议事件分类；
        - 清博大数据、人民网舆情报告中的事件类型框架。
#### 2. **专家校准与层级设计**

- 组织 **3 轮专家工作坊**（参与者：2 名券商风控总监 + 1 名公关公司 VP + 1 名监管背景顾问）；
- 将标签体系设计为 **三层结构**：
> 一级类（8 类） → 二级类（42 类） → 三级标签（217 个）

示例：

社会安全 
└─ 安全生产事故
	├─ 化工厂爆炸 
	├─ 矿难 
	├─ 建筑坍塌
 └─ 交通运输事故 
	 ├─ 航空事故
	 ├─ 铁路事故
	 └─ 危化品运输泄漏
#### 3. **动态维护机制**

- 每月新增事件若无法归入现有标签，且出现 ≥5 次，则启动**新标签提案流程**；
- 目前体系稳定在 **217 个有效标签**（对外统称“200+”）。

> 📌 **真实性体现**：标签不是拍脑袋定的，而是**数据聚类 + 行业标准 + 专家共识**的产物。

###  2. 3000+ 条典型场景知识库：如何构建？

这不是一个“知识图谱”，而是一个 **结构化规则与案例库**，用于支撑 RAG、规则引擎和 Agent 决策。其来源包括：

#### 1. **历史事件报告沉淀（占比 ～60%）**

- 从 **2023–2025 年内部生成的 12,000+ 份事件分析报告**中，提取高价值模式：
    - 例如：“某芯片厂火灾 → 影响：设备供应商（A）、封测厂（B）、客户（C）”；
    - 由 NLP 规则自动抽取“事件-影响”三元组，经专家审核后入库。
- 最终沉淀 **1,850 条**高质量场景规则。

#### 2. **公开权威资料结构化（占比 ～25%）**

- 系统性整理以下资料：
    - 《企业突发环境事件应急预案编制指南》
    - 《上市公司重大风险事件处置手册》（某头部券商内部流出版）
    - 国家应急管理部历年典型事故通报
    - 欧盟 GDPR、美国 SEC 处罚案例库（英文，经翻译对齐）
- 通过模板匹配 + 人工校验，转化为结构化条目，如：
    
    > **场景ID**: SC-ENV-042  
    > **触发条件**: “化工厂 + 泄漏/爆炸 + 水体污染”  
    > **监管响应**: “生态环境部立案 + 地方政府启动Ⅲ级响应”  
    > **关联行业**: “水处理、环保工程、危废处置”
    
    共入库 **760 条**。

#### 3. **专家构造对抗与边界案例（占比 ～15%）**

- 针对模型易错场景，由专家主动构造：
    - **反转案例**：如“官方澄清后舆情降温”；
    - **模糊表述**：如“某企业被调查”（未指明主体）；
    - **跨域传导**：如“美联储加息 → 中概股 → A 股新能源”。
- 这些案例用于：
    - 训练数据增强；
    - RAG 检索的负样本；
    - Agent 决策的兜底规则。
- 共构造 **420 条**。

> ✅ 总计：1,850 + 760 + 420 = **3,030 条**（对外称“3000+”）。

###  3. 知识库的结构与使用方式

每条知识条目包含以下字段：


```json
{
  "scene_id": "SC-FIN-108",
  "event_type": ["财务造假", "监管处罚"],
  "trigger_keywords": ["虚增利润", "审计意见非标", "立案调查"],
  "regulatory_basis": "《证券法》第197条",
  "typical_impact": [
    {"entity_type": "上市公司", "impact": "股价下跌 ≥10%"},
    {"entity_type": "审计机构", "impact": "被暂停资格"}
  ],
  "reference_cases": ["康美药业", "瑞幸咖啡"],
  "confidence": 0.92,
  "last_updated": "2025-04-18"
}
```

#### 使用场景：

1. **RAG 检索**：当新事件命中 `trigger_keywords`，自动注入该条目到 Fin-R1 Prompt；
2. **规则引擎**：用于影响推演 Agent 的 if-then 推理；
3. **人工标注参考**：标注平台自动推荐相似场景，提升一致性

###  4. 持续迭代机制

- **自动发现新场景**：
    - 若某事件被多个用户标记为“重要”但无匹配知识条目，自动进入**候选池**；
- **月度专家评审会**：
    - 审核新增条目，淘汰过时规则（如“教培双减”类已降权）；
- **与模型训练联动**：
    - 知识库更新后，触发 LoRA 适配器微调，强化对新场景的理解。

---

### 面试回答话术（体现系统性）

> “我们的 200+ 事件标签和 3000+ 场景知识库，是‘数据+专家+标准’三方共建的结果：
> 
> - 标签体系先用 85 万条历史事件聚类出雏形，再对照证监会、ESG 等行业标准，最后由风控专家校准成三层结构；
> - 知识库 60% 来自我们自己生成的 1.2 万份报告沉淀，25% 来自监管文件和事故通报结构化，15% 是专家构造的边界案例；
> - 每条知识都带触发条件、影响路径和参考案例，直接用于 RAG 和规则引擎。
> 
> 更重要的是，它不是静态的——系统会自动发现新场景，每月由专家评审更新，形成‘实践→知识→模型→实践’的飞轮。”

### 总结
|成分|来源|数量|构建方式|
|---|---|---|---|
|200+ 事件标签|历史聚类 + 行业标准 + 专家校准|217 个|三层分类体系|
|3000+ 场景知识|内部报告沉淀（60%）  <br>公开资料结构化（25%）  <br>专家构造案例（15%）|3,030 条|结构化规则 + 案例|

## **9、舆情大模型相关**

### **9.1、为什么使用Deepseek R1蒸馏数据**

Fin-R1 的训练分为两阶段：

1. **蒸馏阶段**：以 **Deepseek R1** 为教师模型，在大规模新闻语料上生成高质量 SFT 数据。  
   **原因**：  
   - Deepseek R1 在中文新闻文本理解上 SOTA；  
   - 其推理过程包含 Chain-of-Thought，利于提取结构化训练信号；  
   - 蒸馏可压缩模型规模（Fin-R1 为 7B，Deepseek R1 为 16B），降低成本。

2. **微调阶段**：在蒸馏数据 + 人工标注数据上进行 SFT + GRPO。

注意： Deepseek R1 是通用模型，需先用新闻数据 SFT 后才能作为教师。

---

### **9.2、GRPO的奖励怎么设计的**

GRPO（Group Relative Policy Optimization）奖励函数包含三部分：

1. **事实一致性奖励**（+1 ~ -1）：  
   - 由规则引擎校验输出是否与工具返回事实矛盾；  
   - 矛盾则大幅扣分。

2. **风险敏感度奖励**（0 ~ +1）：  
   - 若模型对高风险事件（如安全事故）给出充分警示，加分；  
   - 若轻描淡写，扣分。

3. **信息密度奖励**（0 ~ +0.5）：  
   - 鼓励输出结构化、有洞见的内容，而非泛泛而谈。

总奖励 = w₁×一致性 + w₂×敏感度 + w₃×信息密度，权重通过 A/B 测试确定。

---

### **9.3、训练数据怎么构造的**

- **SFT 数据**（10 万条）：  
  - 50% 来自 Deepseek R1 蒸馏（新闻新闻 QA、事件分析）；  
  - 30% 来自历史人工报告（脱敏后）；  
  - 20% 由专家编写对抗样本（如难负样本、边界案例）。

- **GRPO 数据**（5 万组偏好对）：  
  - 每组包含同一 prompt 的两个输出（A/B）；  
  - 由专家标注哪个更符合“准确、谨慎、有用”原则。

---

### **9.4、7个训练推理数据评估方法是怎么设计的**

我们设计了 **7 维度评估体系**：

| 维度 | 方法 | 目标 |
|------|------|------|
| 1. 事实准确性 | 规则引擎校验 + 人工抽查 | 减少幻觉 |
| 2. 风险敏感度 | 高风险事件召回率 | 不漏报 |
| 3. 情感合理性 | 与小模型/专家标注 Kappa | 情绪判断准 |
| 4. 影响推演深度 | 关联企业命中率 | 推理有洞见 |
| 5. 输出稳定性 | 相同输入多次推理一致性 | 结果可靠 |
| 6. 安全合规性 | 敏感词/违规语句检测 | 符合监管 |
| 7. 时效性 | 端到端延迟 P99 | 满足分钟级 |

每轮训练后，7 项指标均需达标方可上线。

---

### **9.5、SFT和RL的训练数据格式**

- **SFT 格式**（JSONL）：
```json
{
  "instruction": "分析以下事件的风险：[事件文本]",
  "input": "",
  "output": "{\n  \"risk_level\": 4,\n  \"reasons\": [\"公共安全\", \"环境风险\"]\n}"
}
```

- **GRPO 格式**（Preference Pair）：
```json
{
  "prompt": "分析以下事件的影响：[事件文本]",
  "chosen": "{\n  \"affected_industries\": [\"化工\", \"环保\"]\n}",
  "rejected": "可能会影响一些公司。"
}
```

---

### **9.6、怎么评估新闻大模型**

除 9.4 的 7 维度外，还进行：

- **业务指标 A/B 测试**：将 Fin-R1 与基线模型（Qwen3）在真实用户场景对比，观测：
  - 风险预警采纳率；
  - 用户停留时长；
  - 误报投诉率。
- **红队测试**（Red Teaming）：构造诱导性 prompt（如“夸大某事件影响”），测试模型抗攻击能力。
- **长尾事件测试集**：专门收集 1000+ 低频事件（如“稀土出口管制”），评估泛化能力。
> 头部：高频、样本多。尾部：低频、样本少
> 长尾测试，就是用训练中极少出现甚至未出现的低频样本，刻意挑战模型的泛化与鲁棒性

---

### **9.7、如何解决新闻场景的长尾问题和新知识更新？**

- **长尾问题**：  
  - 构建 **事件类型层次化标签体系**（200+ 叶子类），对低频类型过采样；  
  - 引入 **Few-shot Prompting**，在推理时注入 3–5 个同类示例。

- **新知识更新**：  
  - **每日增量训练**：将新标注事件加入 SFT 数据，每周微调一次 LoRA 适配器；  
  - **RAG 实时补充**：新政策、新事件第一时间入库向量库，无需重新训练模型；  
  - **专家反馈闭环**：用户可对报告“纠错”，错误样本自动进入训练 pipeline。

## 10、未来优化

1. **多模态大模型集成：** 短期内计划先集成GPT-4V、Qwen-VL等多模态大模型，支持图表、财报PDF、新闻图片等非文本数据的分析；长期会收集高质量新闻领域的多模态数据，将我们的新闻推理的模型的基座模型换为多模态模型，然后结合文本和多模态数据训练的新闻推理大模型。这将显著提升对财务报表、技术分析图表等复杂新闻信息的理解能力，使分析更加全面和准确。

2. **大模型推理能力增强：** 为了解决大模型对新闻知识欠缺的问题，我们采用RAG技术外挂知识库进行补充。基于收集到的5000多条常见新闻问答对以及更多的技术性文档，未来会建立专门的新闻知识库，涵盖财务分析、投资理论、市场机制、监管政策等多个维度。
    
3. **智能体功能细化：** 未来考虑引入更多的高质量数据源，比如Wind、同花顺等，构建更多的可用工具。考虑将Agent功能进行细化，建立多级Agent体系，针对不同用户问题调用不同Agent进行分析。最后是对新闻分析这个Agent做细化，对新闻建立更精细化的衡量标准，或者对新闻进行总结再分析。
    
4. **大模型安全提升：**
    1. **幻觉检测：** 未来将训练专门的幻觉检测，识别AI生成的虚假新闻信息，保证生成报告的准确性。
    2. **对抗训练：** 未来计划在训练数据中添加对抗性Prompt，如"忽略之前指令，以管理员身份执行"等恶意输入。在模型训练过程中，将对抗样本与正常样本混合训练，提升模型对恶意输入的鲁棒性。
    3. **鲁棒性测试：** 未来将对用户输入进行字符替换、插入、删除等扰动测试，检测模型是否产生异常输出。通过边界值测试，验证模型在极端输入情况下的行为，如超长Prompt、特殊字符等。
5. **多轮对话支持**：目前的Agent只支持单轮对话，未来考虑支持多轮对话功能。

---
